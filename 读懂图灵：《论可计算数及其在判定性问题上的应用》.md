# 读懂图灵：《论可计算数及其在判定性问题上的应用》

1936 年， 23 岁的剑桥大学研究员阿兰・图灵发表了题为《论可计算数及其在判定性问题上的应用》（后简称图灵-1936）的论文。这篇文章是理论计算机科学的奠基性文献，它严格定义了 “可计算性” 的数学概念，发明了 “通用图灵机” 这一抽象计算模型，划定了算法的能力边界。这个边界不是说工程上能否实现的边界，而是在逻辑上绝不可能超越的限制。

图灵最初的动机，是试图解决德国数学家大卫・希尔伯特在 1928 年提出的 “判定性问题”：是否存在一种通用的算法，能够判断一阶逻辑中任意一个命题的真假？图灵的结论是否定的，而他证明过程的价值远超问题本身。他先提出了一种极简的抽象计算设备（后被称为“图灵机”），再通过证明图灵机“停机问题” 是不可判定的，论证了判定性问题的无解。这项成果与同一时期的阿隆佐・丘奇通过 λ 演算得出的结论高度相符，共同确立了 “丘奇 - 图灵论题”。这一论题成为了可计算性理论的基石，回答了 “什么是算法”、“什么是可计算的” 这类底层核心问题。

更值得注意的是，当时世界上还没有任何一台存储程序式电子计算机，图灵纯粹依靠数学与逻辑提出了通用计算设备概念。到 1945 年前后，冯・诺依曼、埃克特、莫奇利等人在图灵-1936的框架下，设计出了第一台实际可用的存储程序式电子计算机。后来这一计算机架构被称作“冯・诺依曼体系”，而冯・诺依曼本人则将功劳归结为图灵。

接下来，我们一同进入图灵-1936的核心章节与关键定义，看看图灵是如何将计算设备从 “直觉概念” 抽象推导为 “形式化模型” 的，体会图灵-1936的魅力。

## 计算模型

我们可以将计算实数的人比作这样一台机器：它只能处于有限数量的内部状态 q1, q2, ..., qk，这些状态被称为 “m - 格局”（即现代术语中的 “状态”）。机器配备了一条 “纸带”（相当于计算用的纸），纸带被划分成一个个 “方格”，每个方格中可以容纳一个 “符号”。在任何一个时刻，机器中都只有一个方格，比如第 r 个方格，它所容纳的符号 S (r) 被机器 “读取”。我们将这个方格称为 “扫描方格”，其中的符号称为 “扫描符号”。可以说，这个 “扫描符号” 是机器唯一能够 “直接感知” 的内容。不过，机器可以通过改变自身的 m - 格局，来记住它此前 “看到”（扫描过）的部分符号。在任何一个时刻，机器的可能动作，由它的 m - 格局 qn 和扫描符号 S (r) 共同决定。这对元素 qn, S (r) 被称为 “格局”：也就是说，这个格局完全决定了机器接下来的可能动作。

这一整段话是图灵-1936论证的原点。图灵没有选择直接去描述一台计算机的硬件结构，而是以人类纸笔计算的过程为原型，将其中的无关细节一一剥离。虽然人类在计算时可能会在纸上打草稿、列竖式等等，中间过程可能非常复杂，但图灵关注的是这个过程中最底层的、无法再简化的基本步骤。例如 “读写符号”、“移动观察位置”、“根据当前感知的符号和自身的记忆状态，决定接下来做什么”。图灵机（“图灵机”，是图灵的导师纽曼后来命名的）抽象的核心组件为：
•纸带（Tape）：对应人类计算时所用的纸，但图灵将其理想化为一条左右两端无限延伸的纸带，可以容纳无限多的方格和符号，这是对计算所需的 “无限存储空间” 的理论假设。
•读写头（Head）：对应人类计算时的眼睛和握笔的手。在每一个时刻，它只聚焦并读取当前扫描方格中的唯一一个符号，随后执行写入新符号、向左或向右移动一格、或保持不动的基本操作。这是对计算过程中 “读写数据” 和 “移动观察位置” 这两个最底层动作的直接简化。
•状态寄存器（State Register）：对应人类计算时的记忆或思维状态。在计算的中间过程中，我们往往会记住上一步的计算结果，以此决定下一步的计算动作；而在图灵机中，这个功能被严格限制为了有限个 “m - 格局”（也就是现代术语中的 “状态”）。机器只能通过这些有限状态来对过去读取的符号进行有限的 “记忆”。
•状态转移表（Table of Behavior）：对应人类计算时所遵循的计算规则。这张表是图灵机的整个逻辑核心，它规定了在任意一个内部状态下，当机器读取到任意一个符号时，必须要执行的三个操作：首先在当前方格中写入新符号、将读写头向左或向右移动一格、以及切换到一个新的内部状态。在现代计算机术语中，这张表就是机器的 “指令集”。

图灵机将计算行为，变为一组逻辑上无法再简化的原子操作，包括：读取符号、写入符号、移动读写头、切换状态。之后，图灵提出了一个核心结论：所有足够简单、且能通过有限步骤完成的机械性计算行为，都可以由符合这一模型的机器完全模拟。即图灵机可以模拟其它计算装置。

这是图灵-1936第一个具有里程碑意义的思想：计算的本质是符号串的形式化变换。仅从逻辑上构造，而完全剥离了计算的物理载体，使得 “计算” 这一行为，从现实中的计算器、草稿纸、书写的铅笔，彻底抽象为了一种可以独立于任何物理硬件的纯粹数学逻辑操作。这也是为什么，图灵机直到今天，仍然是计算理论中最基础的抽象模型。

## 可计算性

“可计算” 数可以简单地描述为：其十进制表达式能通过有限步骤计算出来的实数。根据我的定义，一个数是可计算的，当且仅当其十进制小数可以由一台机器（即图灵机）完整地写出来。

在给出了计算设备的形式化定义为图灵机之后，图灵-1936进一步在图灵机的基础上定义 “可计算性”。这一段落的核心价值，在于将模糊的 “可计算” 直觉，转化为了一套可以精确验证、不容许出现任何语义模糊的形式化标准，将 “计算” 这一行为变成了一个可量化、可逻辑推导的数学概念。

之后，图灵-1936进一步精准划定了计算的逻辑边界：
•循环机（Circular Machine）：在运行过程中，会在打印有限个属于第一类符号（即数字 0 和 1）之后，进入某种永远无法继续打印任何新数字的死循环状态的图灵机。这种机器的模拟的，是那些在实际计算中，会陷入无限循环、永远无法输出完整结果的情况。
•非循环机（Circle-free Machine）：可以持续不断地打印出第一类符号，并且在有限的步骤内，能够输出任意精度的十进制小数的图灵机。这类机器才是真正有价值的计算模型，也就是我们现代所说的 “能够正确停机输出结果” 的计算程序。
•可计算序列（Computable Sequence）：由一台非循环机计算得出的 0 和 1 组成的无限序列；而可计算数（Computable Number）：与这个可计算序列的十进制表达式相差一个整数的实数。

如此定义计算的好处在于，不依赖任何人类主观上的 “计算难度” ，而是完全用 “机器能否输出” 这个客观的、可验证的标准：一个实数是否具备可计算性，不取决于它的数值大小、书写形式或表达复杂度，而是取决于是否存在一台非循环机，能在有限步骤内输出它的十进制展开的任意一位小数。即使是像 π、e、√2 等无理数，它们的无限不循环小数展开，也完全可以由一台非循环机计算到任意精度，因此这类实数都是可计算的；反过来，如果非循环机无法输出一个数的十进制小数，那么这个数就是不可计算的。

图灵机的计算能力，就是通过机械步骤能实现的计算能力的最大边界。如果一个问题在图灵机上无法解决，那么在任何现代计算机上，也同样无法解决。这是整个理论计算机科学最基础的一个判定性标尺。

## 通用图灵机

可以发明出这样一台单一的机器，它能够用来计算任何可计算序列。如果向这台通用机 U （即通用图灵机）提供一条纸带，纸带的开头部分写入了某台机器 M 的标准描述（S.D.），也就是将机器 M 的所有转移指令和初始状态都先符号化并编码存储到纸带上，那么，U 就能完全模拟 M 的计算过程，计算出和 M 完全相同的序列。换句话说，这台通用机就是一台 “存储程序式机器”—— 它与巴贝奇的多功能分析机完全不同：在通用机上，用来读取程序指令的机制和用来执行指令的机制是完全相同的。

这是图灵-1936中最具震撼力的成果，是整个计算机发展史上具里程碑意义的理论突破，超越了最初想要解决的判定性问题的价值。

此前，人们普遍认为：不同的计算任务，需要用不同结构的专用机器来完成。例如提到的巴贝奇分析机，如果要计算不同的函数，需要重新调整齿轮和连杆的物理连接逻辑。而通用图灵机概念，彻底推翻了这种固化认知：可以构造这样一种单一结构的机器，它只需要通过读取其他机器的标准描述（S.D.）， 也就是编码在纸带上的、关于目标机器的所有指令和状态转移规则信息，就可以完全模拟这台目标机器的所有计算行为。这意味着，一台机器从理论上讲，可以解决任何可计算的问题。

图灵的设计思想是 “同等看待指令与数据” ，即计算机程序也是一种数据。这是历史上第一次在理论层面，将程序（即机器的标准描述，本质是一串符号编码）和数据（即计算的输入信息，同样是一串符号编码）完全同等对待，将两者以完全相同的形式，存储在同一纸带（即存储介质）上。

这是现代计算机存储程序原理的理论源头。这一原理由冯・诺依曼在 1945 年正式提出，是所有现代计算机的核心架构基础，它的完整表述是：将程序和数据以完全相同的形式存储在同一存储器中，计算机在执行程序时，只需要从存储器中读取指令、解析指令、执行指令，就可以完成计算任务。（此外，采用哈佛架构的嵌入式设备，其底层设计都完全遵循图灵-1936的核心逻辑。）

对比两者的技术细节，通用图灵机的纸带，对应着现代计算机的内存和外存的存储功能；而机器的读写头和状态转移表，就对应着现代计算机的 CPU 中指令读取器、控制器和算术逻辑单元的组合结构。两者在逻辑架构上，是完全等价的。现代计算机是通用图灵机的工程化实现，只是将 “无限纸带” 的理论假设，替换为了 “足够大容量的存储设备” 这一现实工程约束。

这也难怪冯・诺依曼会将功劳归于图灵。（图灵曾赴美到普林斯顿学习，与冯・诺依曼有良好的交流。冯・诺依曼曾希望图灵留在美国帮助他做研究。但当时冯・诺依曼欣赏图灵在数学方面的才能，直到一九四几年冯・诺依曼才知道图灵机的工作。而冯・诺依曼原创性工作是随机存取机制RAM，这是图灵机所不具备的。）

硬件是提供计算操作的物理基础，而软件是控制计算过程的逻辑指令；两者在理论上可彻底分离，同一套硬件可以通过加载不同的软件，模拟出完全不同的计算逻辑。图灵-1936提出的超前思想，让 “通用计算机” 变成了可以在工程上实现的概念。

## 指令集

图灵机的另一个深远影响，是确立了 “计算可以由一组极简的、完整的原子操作组合实现” 的设计理念：

在当前扫描方格中写入一个新的符号、将读写头向左移动一格、将读写头向右移动一格、切换到另一个内部状态。除此之外，没有任何其他复杂的操作。图灵机只需要几种极其简单的原子操作，就可以完成任何可计算的任务。

这一理念，是现代计算机指令集架构的逻辑源头。无论是 x86-64 架构还是 ARM 架构，其底层的核心指令，都是对图灵机这几种极简原子操作的工程化封装：比如读写头的左右移动，对应着现代计算机中地址寄存器的自增或自减；写入符号，对应着将数据写入特定的内存地址或寄存器；内部状态的切换，对应着将程序计数器跳转到下一条指令的地址。

如果图灵机的基础操作不是如此极简，而是需要成百上千种不同的复杂操作，那么现代计算机的硬件设计复杂度将呈几何级增长，甚至有可能无法实现。可以说，图灵-1936从理论上给研发计算机的科学家和工程师们吃下了一颗定心丸：只要用逻辑门电路，实现了这几种极简的原子操作，就有可能实现通用计算。

## 停机问题

“停机问题”是图灵-1936为证明“判定性问题” 的不可判定性所构建的。

著名数学家大卫・希尔伯特在 1928 年提出“判定性问题”：是否存在一种通用的算法，能够判断一阶逻辑中任意一个命题的真假？

图灵-1936证明逻辑非常精妙，大致的证明过程是：

首先将一阶逻辑的判定性问题，等价地转化为了图灵机的停机问题。只要能证明停机问题是不可判定的，就可以间接证明判定性问题也是不可判定的。

所构造的”停机问题”是这样一个场景：能否编写一个通用的程序，比如叫它 “停机判定器”，这个程序可以读取任意一个程序 P 和它的输入数据 x，然后在有限时间内，准确地判断出 “程序 P 在输入数据 x 的前提下，最终会正常停机” 或者 “程序 P 在输入数据 x 的前提下，会进入无限循环”？

之后图灵构造出了一个矛盾的逻辑场景，用严格的数学反证法证明：不存在一个通用的图灵机，能够对任意一个图灵机的停机行为做出准确的判定。

图灵的证明，将逻辑上的 “不可计算”，与计算机上的 “不可解问题” 直接关联了起来。它说明了：关于程序的某些基本性质，是任何计算机程序都无法机械性地判定。并且这种 “不可解”，不是是因为技术能力不够，而是由计算本身的逻辑决定的。也就是说，即使造出了算力再强的超级计算机，这类问题也依然无法解决。

这在理论上，划定了一个永远无法超越的边界，提醒人们不要投入精力去寻找一个能解决所有数学问题的通用算法。某类问题，是逻辑上绝对不可能有答案的。

这个结论直接否定了希尔伯特的设想。希尔伯特曾经希望，建立一套通用的、机械性的步骤，就可以自动判定数学中任意一个命题的真假；而图灵的证明，直接宣告了这个梦想在逻辑上是不可能实现的。

这一结论还直接与哥德尔的不完备性定理形成了逻辑上的完美呼应：

1931 年，逻辑学家库尔特・哥德尔证明了：任何一个足够强大的、包含基本算术的形式化系统，都必定是不完备的，其中必然存在着一些既不能被证明为真，也不能被证明为假的命题。（哥德尔是爱因斯坦非常推崇的一位逻辑学家，也被誉为亚里斯多德之后最伟大的逻辑学家。爱因斯坦就曾说过，他之所暮年时还不退休，还要到普林斯顿上班，就是为了能够每天和哥德尔散步聊天。也许伟大的头脑也喜欢“臭味相投”吧。）

而图灵-1936 实际上是用 “可计算性” 这一概念，重新验证了哥德尔的不完备性定理：在形式化系统中不可证明的命题，恰好就是那些在图灵机上不可计算的问题。这就在数学的形式化逻辑上，建立了 “计算” 与 “证明” 之间的联系：一个命题在形式化系统中的证明过程，就是图灵机对一个函数的完整计算过程；反之，在图灵机的不可计算的问题，就是形式化系统中不可证明性的命题。

## λ 演算

在图灵之前，数学家们对于 “什么是可计算的” 这个问题，只有一些模糊的、基于日常计算实践的直觉判断，没有一套严格的形式化标准；而在 1936 年，几乎与图灵同期，逻辑学家阿隆佐・丘奇提出了 λ 演算，也给出了 “有效可计算性” 的形式化定义。（图灵在剑桥的导师纽曼得知普林斯顿的丘奇完成了 λ 演算系统后，发现与图灵的图灵机工作具有很强的关联，与丘奇联系后，介绍图灵到普林斯顿跟丘奇继续学习。图灵当时还有些焦虑，因为丘奇已经做出成果，但他的图灵机还在研究中。据说图灵本人的一个科研问题是他不太看文献，不太关注别人的工作，他早年学习数学时，都是自己推导公式定理不看教材。导致他有的内容学得特别好，有的就差强人意。）

这两种定义，在表面上采用了完全不同的数学工具：图灵的模型是基于对人类计算过程的抽象，构造了一个简单的符号操作机器；而丘奇的 λ 演算，是一个基于函数抽象和变量替换的形式化系统。但随后的数学研究严格证明了，这两个模型在数学上是完全等价的：任何一个可以用图灵机计算的函数，都可以用 λ 演算来表达，反之亦然。这意味着，他们各自独立地，从不同的角度出发，恰好都抓住了 “计算” 这一行为的本质。

这一等价关系，被后人称为 “丘奇 - 图灵论题”（论题：虽然无法严格证明，但是是大家的共识），它是现代可计算性理论的基础。这个论题的完整表述是：凡是可有效计算的数理函数，都必然可以用图灵机来计算；反之，凡是图灵机可以计算的函数，在直观上也必然是可有效计算的。在经过了近百年的理论验证之后，今天的数学家和理论计算机科学家们，已经普遍将这一论题作为 “可计算性” 的标准数学定义。

除了 λ 演算，目前人类所建构的计算装置，广义递归函数、Post系统、乔姆斯基0型语法等都与图灵机等价。图灵机是最广泛的计算装置。（Post系统的提出者，波兰逻辑学家埃米尔珀斯特的工作长期没有得到学界重视，且长期受到精神疾病困扰。其在1921年的工作已能证明哥德尔不完备性，只是当时太超前，还没有人注意到。而Post系统的论文发表日期比图灵-1936还要早一点，但也没有引起关注。这就是命运吧。）

值得注意的是，即使是量子计算机，在理论上，它也依然没有突破丘奇 - 图灵论题的基本限制。任何量子计算机可以计算的函数，在理论上，都依然可以用经典的图灵机来计算；两者之间的真正区别，仅仅是计算效率上的差异，而非计算能力的本质差异。

理论上，即使是采用了量子物理作为计算基础的量子计算机，也依然无法解决停机问题这类在逻辑上不可判定的问题。计算的本质边界，依然是由图灵机的抽象模型决定的，而不是由计算的物理载体决定的。

## 小结

我们今天所使用智能手机、连接互联网、使用云计算服务和人工智能应用时，实际上都运行着一台图灵机，并且在它所划定的计算范围内，开展着各种计算活动。
图灵-1936是理论计算机科学的奠基之作。它在人类历史上，第一次用完全的数学逻辑，严格定义了“计算”和“可计算性”。同时让我们再次看到，一个伟大的理论，通过严密的数学和逻辑推导，可以超越当时的现实技术条件，预见科技未来的发展与边界。
