# 读懂计算思维：计算机科学求解问题的基础

在今天讨论“计算思维”，是因为“像计算机科学家一样思考”这种宽泛的感知无法满足专业和体系化的实践需求，也因为大模型等人工智能技术大行其道，我们需要应用“计算思维”更好地体现人的价值，而不是像计算机一样思考，更不是用计算机替代人的思考。

为引起重视，也是为了更好地理解为什么我们需要计算思维。下面第一部分，简要梳理计算思维的正式确立以及一些演变的例子。

2006 年 3 月，时任美国卡内基梅隆大学计算机科学系主任的周以真教授，在计算机学科顶级学术期刊《Communications of the ACM》（《ACM 通讯》）上发表了题为《Computational Thinking计算思维》（《计算思维》）的论文，将分散在算法设计、软件开发、系统架构等各个技术维度的底层逻辑，抽象、提炼为一套思维理论体系，正式将计算思维确立为计算机科学领域的核心认知。

什么是计算思维？运用计算机科学的基础概念进行问题求解、系统设计以及人类行为理解的一系列思维活动。

怎么理解这个概念呢？

首先，计算思维是人类面对复杂问题时主动采用的一种认知，而不是要求人类像计算机那样去思考。目的就是运用计算机科学的概念和思维设计出求解问题的合理逻辑。

其次，什么是求解问题的合理逻辑呢，如何解决生活中的问题呢？用数学思维完成问题的形式化建模，以工程思维兼顾资源效率与实际场景约束，从而让解决方案从 “理论可行” 转化为 “实际可落地”。

需要什么能力才能设计出这种合理逻辑？需要具备多层次的抽象逻辑能力，以及对问题本身的形式化解构能力。

我不是计算机科学家，也不是计算机相关从业者，计算思维和我有关系吗？计算思维是属于所有人的思维工具，用计算的思想和逻辑去分析问题、构建求解路径的认知过程。计算机科研和产品开发只是计算思维活动的副产品。

举些生活例子：学生根据今天的课程表整理书包，等价于计算机系统中典型的预取与缓存（Prefetching & Caching）策略；超市收银台开放多个收银通道，等价于计算机系统中多核心架构下的负载均衡（Load Balancing）策略。

而随着计算机技术的持续迭代，计算思维的表现形式也在演变。例如2010年后，随着数据密集型计算场景的快速普及，数据的价值被进一步放大，计算思维中又逐步分化出了一种新的针对性落地形态 —— 数据思维。

也有人会问，在人工智能，特别是生成式人工智能直接能给出“答案”的今天，我们是否还有必要掌握计算思维？

2024 年 8 月，《ACM 通讯》发表的一篇题为《Leveraging Computational Thinking in the Era of Generative AI》（《在生成式人工智能时代利用计算思维》）的深度分析文章指出：恰恰是生成式人工智能技术的普及，进一步放大了计算思维的价值。生成式人工智能技术目前仍无法真正理解问题的核心本质，这一短板，正是计算思维的核心价值域，也是人类必须具备的核心认知能力。


2026 年 CACM 的相关研究指出，在 Vibe Coding 模式下，开发过程几乎完全是通过与 AI 的自然语言对话完成的，整个过程的核心，不再是编写具体的编码语句，而是用计算思维的逻辑，精准地将业务场景需求转化为人工智能可以理解的、形式化的、具有明确执行边界的描述。这意味着，计算思维的落地形态，已经完全脱离了传统编码的技术性约束，回归求解问题的本质。

既然如此重要，那计算思维有哪些具体内容呢？接下来的第二部分内容，综合国际上具有代表性的学术研究和产业实践经验，将计算思维归纳为三大类、十项思维：

第一类：核心问题求解思维。这是计算思维最基础、最本质的内容，是在解决问题时，遵循的底层逻辑。

第一项是问题拆解：在面对复杂的、无法直接求解的大规模问题时，需要将其按照逻辑，系统性地分解为若干个规模更小、逻辑更清晰、便于独立处理的子问题；随后，再对子问题进行重复拆解，直到拆解后的最小子问题，具备成熟的、可直接复用的解决方案。

问题拆解不是简单的物理性拆分，而是一种需要遵循内在逻辑约束的技术性分解。拆解的过程，必须保证子问题之间的逻辑是解耦的，而每个子问题内部的逻辑又是具有高度内聚性的。

例如，电商平台的订单处理系统，通常会将整个订单流程，拆解为订单创建、支付扣款、库存扣减、物流通知、账单生成等多个独立的核心子服务，每个子服务都包含明确的独立业务逻辑；而在应对海量用户的高并发请求场景时，将每个子服务的流量，按照用户唯一标识或业务唯一标识，进一步拆分为多个独立处理的分片，部署到不同的服务器节点上处理，以此分散系统的资源负载，提升整个系统的整体并发处理能力。

第二项是抽象化：在完成问题拆解后，从与问题求解目标相关的大量的复杂场景细节或干扰信息中，精准提取出与求解目标相关的核心关键信息，用简洁的、形式化的数学模型或其他逻辑模型表示出来，形成一个通用的、可复用的、不受特定场景约束的标准化问题。简而言之，就是抓住问题的 “主要矛盾”。

例如，电商平台的商品库存系统，首先需要从商品的日常管理场景中，提取出 “库存数量”“库存锁定时间”“库存变更流水记录” 等核心关键数据，以及对这些数据的 “增加库存”“减少库存”“查询库存” 等核心操作逻辑，封装为一个库存模型，用它来表征商品库存管理活动。

在抽象化中，暗含了一种重要的计算思维：模式识别：识别数据、问题之间的共同特征、规律。广泛使用的语音识别、图像识别、机器翻译等等，都属于模式识别。

第三项是算法设计：在完成抽象化后，设计出一套执行步骤。算法的执行步骤必须在有限的次数、有限的时间内完成，每一步的执行逻辑必须是精确定义的，不能存在模糊性的表述，并且必须在当前的计算资源约束下完成实际落地。

例如，一个支持高并发场景的库存扣减操作，整个业务流程可拆解为 “查询现有库存”“检查库存是否足够扣减”“预扣减库存”“将扣减结果持久化到数据库”“记录库存变更流水” 等多个明确的执行步骤。在面对大规模的并发请求流量时，可以通过对请求的业务唯一标识进行编码标识映射节点计算，将同一业务标识请求分配到同一个处理节点上，在保证资源负载均衡的前提下，提升系统的缓存命中率，降低后端的资源处理压力。而在扣减同一商品库存时，多个处理节点需要争夺唯一资源凭证，争取到的处理节点执行扣减操作，其他节点则进入等待资源凭证释放后再进行争抢。这使得并发变为线性，保障扣减数目的正确性。

事实上，在20 世纪 50到60 年代，计算机科学尚未完全从数学中独立，计算机学科先驱们大多都是数学家，他们为了厘清计算机与数学在问题解决场景中的核心边界，将算法思维作为本学科的标志性思维模式。那时起，算法思维被定义为 “以形式化规则界定问题求解步骤，确保方案在有限步骤内机械执行”  。这一定义内涵，直到今天仍是算法的基础。下面是一些具体的算法设计思维：

分治算法：是将一个大规模的、难以直接求解的问题，拆解为多个规模较小的、逻辑上独立的、可以用同一逻辑单独求解的子问题，再将各个子问题的求解结果，合并为整个问题的最终结果;

贪心算法：是在对问题求解的过程中，总是选择在当前看来是最优的解决方案，逐步逼近整个问题的全局最优解 ；

启发式算法：和贪心算法类似，我们也许无法得到最优解，或者在现实条件下一时无法得到最优，那么逐步挨近，逐步靠近，得到一个够好的解，也是可行的。

动态规划算法：是将一个复杂的问题，拆解为多个简单的、可以被复用的子问题，通过对这些子问题进行重复求解，记录每个子问题的求解结果，保证后续遇到同样的子问题时，可以直接复用之前的求解结果，以此避免重复计算，提升算法的执行效率；

回溯算法：是通过尝试不同的路径，来逐步探索问题的所有可能的解决方案，一旦发现当前路径的执行逻辑已经无法满足求解目标，就回退到上一步的执行逻辑，重新选择新的路径继续探索 —— 这是计算思维中 “问题拆解” 和 后续会提到的“迭代”思维。

递归算法：这可以说是计算思维中与传统思维差异最大的思维方法之一。要明白递归，先说递推。递推就是，你要知道1+n的结果，从1+0=1，1+1=2，1+2=3...推出1+n等于多少，从一个个例总结规律，所谓归纳法的一个基础就是递推。而递归则是你要知道1+n等于多少，你只需要知道1+（n-1）的结果再加1即可。那1+（n-1）的结果呢？不用操心，你只需要知道1+（n-2）的结果再加1即可...一直到，你确实知道了1+0=1时，就可以反推回去得到1+n的结果。

从最顶层的问题向下分解成子问题，每个子问题都和顶层问题是同一结构，只是问题规模变小了。直到我们找到了一个有解的问题规模，开始逐步向上解决更上一层的问题，直到解决顶层问题。我们人会觉得这种计算很难，因为好像都不知道中间结果，也不知道要推到多少层，可能时好几百，好几千层。人没法大量做这种计算的。但计算机正好相反，这种虽然步骤很多但逻辑简单明了的计算正适合计算机。

举个例子，经典的登台阶问题：一个人要登上20阶台阶，每次只能跨 1 级或 2 级台阶。请问，他有多少种不同的走法可以登上楼顶？

从递归的角度，要登上20阶，那么就是就是从19阶跨1级，或者18阶跨2；而要登上19阶，那么就是从18阶跨1级，或者从17阶跨2级.....向下推的公式：f[i] = f[i-1] + f[i-2] ,f表示登台阶路径数，一直到i=2时，我们知道f[2]=2，当i=1时，f[1]=1，那么就可以向上递归f[3]=f[1]+f[2]=3，一直向上递归得出f[20]。

递归多说了一些，主要想引起重视：想清楚什么事对人来说很难，但是对计算机而言很简单；什么事对人来说很简单，但是对计算机而言很难。想清楚这一点，往往能帮助我们打开思路。

第四项是模块化：在完成问题拆解后，将具有高度内聚性逻辑的子问题，以及对应的算法实现逻辑，整体封装为一个被称为“模块”的独立单元，明确不同模块之间的相互依赖关系，以及模块之间进行通信的标准。

模块化的核心价值，是彻底降低大型系统的整体逻辑复杂度 —— 它将 “模块内部的实现逻辑” 与 “模块间的协作逻辑” 进行了彻底的隔离。在关注某一个模块时，不需要理解其他所有模块的内部实现细节。

例如，电商平台的交易系统，可拆分为多个独立的、具备单一职责性的模块，比如用户模块、商品模块、订单模块、支付模块、库存模块、物流模块等；各个模块，只需要按照预先定义好的规范，接收特定格式的请求参数，处理完成后返回统一格式的响应结果，就可以完成相互间的业务协同。比如，库存模块只需要对外提供 “扣减库存” 的标准接口，订单模块在完成了订单创建的逻辑后，就可以直接调用这一接口来完成库存扣减，而不需要关心库存模块内部的具体实现逻辑。

第五项是调试，在算法或模块的实际执行结果与预期的结果不一致时，系统地分析、定位并找出问题根源的过程。这就要求对整个系统运行机制的完整理解。

例如，在订单模块调用库存模块的扣减接口时，如果库存扣减的实际结果，与订单模块的预期输入不匹配，那就需要逐步排查整个请求的完整执行路径：从订单模块发起请求的地方开始，依次排查请求是否正确通过了接口、是否正确到达了库存模块的服务、库存模块的内部逻辑是否正确执行、数据库的持久化操作是否正确完成、返回的响应结果是否被正确传输回订单模块等待，才能最终定位到问题的根源。

而调试，暗含了另一种重要的思维：评估，能够及时地检验方案的效果，通过即时的反馈来分析可改进项。通常评估需要和指标结合起来，这是我们能不断改进的指示器。

第六项是迭代，不可能一次性构建出一个完整的复杂系统，也不肯能一次性满足所有需求，解决所有问题，因此需要在不影响核心的前提下，逐步地对系统进行功能补充和优化。通过持续的收集反馈、问题发现，不断对现有方案进行优化和升级。

例如，电商平台的订单系统，通常会先实现一个支撑核心下单流程的基础版本，保证核心业务的正常运行；在后续的迭代版本中，再陆续增加优惠扣减、智能推荐商品、可视化售后工单处理、实时订单数据报表分析等非核心的辅助功能。

以上是六项问题求解的基础思维。接下来介绍第二类计算思维，在设计系统，或者说对整体解决方案进行拓展的三项思维，主要是考虑资源约束、组织与协同的辩证关系。

第一项是并行处理：通过对资源的调度和分配，将一个大规模的、可以并行执行的任务，拆解为多个独立的子任务后，分配给多个处理单元（如多核心 CPU、分布式集群节点）同时执行。利用处理单元和资源的并行性，来大幅缩短整个任务的整体执行时间。这是现代计算机系统架构设计中，应对大规模并发流量或海量数据处理场景的核心技术手段。

例如，支持海量并发流量的电商平台，在请求流量的入口，部署一套具备集群架构的负载均衡服务器集群，将海量的用户请求流量，均匀地分配到后端的多个应用服务器节点上，让多个节点同时并行地处理用户请求，以此提升整个系统的并发处理能力；在后端的数据库层，按照一定的分片规则，拆分到多个独立的数据库分片节点中，当需要对海量数据进行处理时，查询请求会被分发到多个分片节点上并行执行，最后将各个节点的查询结果进行汇总整合，返回给上层应用，以此大幅缩短海量数据的查询响应时间。

第二项是权衡思维：在实际场景中，方案往往会受到多维度的客观条件约束，比如可用的处理单元、预期的时间窗口、团队的能力、第三方约束等；在多个相互制约的维度之间，找到一个合理的方案。

例如，电商平台中的库存扣减场景，对数据的一致性要求极高，那么技术方案就需要优先保证数据的强一致性，即使这会带来一定的性能损耗；比如电商平台中的用户对商品列表查询场景，要求系统具备更高的可用性，但数据一致性要求更底，那么技术方案需要优先保证服务的可用性，允许数据在短时间内存在不一致，比如，可以适当延长缓存的刷新间隔，来降低数据库的访问压力。

第三项是容错与恢复：在设计解决方案时，需要提前对未来可能出现的各种类型的故障，进行充分的预判和评估，并有针对性地设计出相应的冗余方案、故障隔离机制、自动容错和恢复方案。以此保证，在系统部分发生故障的情况下，整个系统仍能正常提供服务，或者在可接受的时间窗口内，快速恢复正常的服务能力。

例如，电商平台的订单系统对可用性要求高，通常会在多个不同的可用区，部署多个完全独立的服务实例节点，每个节点都具备完整的业务处理能力；并在流量入口部署一套健康检查机制，实时对所有服务实例的可用性进行探检测。当其中一个可用区的服务实例发生故障时，负载均衡设备会自动将后续的用户请求流量，切换到其他正常可用区的服务实例上，保证整个系统不会因为局部节点故障，而出现中断服务的情况；而在底层的数据库层，部署多副本的高可用集群，当主数据库节点发生故障时，系统会自动将服务切换到同步的备用节点上，保证数据库层的服务可用性；此外，对核心业务数据进行定期的全量备份以及实时的增量备份，保证在数据出现异常时，可以通过备份文件，将数据恢复到最近的一个正常时间节点，将故障带来的业务影响降到最低。

除了前两类已经广泛应用的计算思维外，在近年来特定技术场景深度发展后，计算思维也演化出的更具针对性的思维模式。下面介绍两项：

第一项是数据思维：计算思维的重心逐步从 “以计算为中心的业务逻辑组装”，转向了 “以数据为中心的价值挖掘”，数据成为了整个系统设计或解决方案的第一对象，包括数据的采集、清洗、存储、索引、检索和价值挖掘。

其核心逻辑是：将计算思维中的抽象化、模块化、并行处理等基础技能，组合应用到数据处理的全生命周期中，通过对数据的系统性组织、处理和分析，挖掘出对业务有实际支撑价值的信息。

例如，电商平台的用户行为分析系统，需要将用户在电商平台内的所有行为数据，比如浏览商品、加入购物车、提交订单、收藏商品等，进行完整的采集，并按照一定的规则，对这些数据进行清洗和格式化处理，将其转换为标准化的格式；再将处理后的海量数据，按照特定的分片规则，存储在分布式文件系统或分布式数仓中；再利用大数据处理框架工具，对这些海量数据进行并行化的分析计算，挖掘出用户的行为偏好、商品的用户群体特征、流量的高峰时间段分布等有价值的业务信息。这整个过程的核心逻辑，是计算思维中 “分而治之” 的并行处理思想，以及对数据的抽象化建模能力。

第二项是提示工程思维：提示工程是将需要解决的复杂技术问题，以及问题的上下文场景信息，用大模型可以理解的、具备足够的明确性、不存歧义、且符合特定格式约束的自然语言，进行形式化的描述和表达，让 AI 工具能够精准理解问题的核心所在，以及技术人员对方案的预期标准，最终生成符合业务需求的技术结果。

从技术逻辑的层面看，构建一个高效的提示词，首先需要将复杂的业务问题，拆解为多个逻辑清晰、粒度合适的子问题；再对每个子问题的核心输入输出、处理逻辑的边界，进行抽象化的表征；再将这些子问题的求解逻辑，以及对应的业务场景约束，按照规范的格式，组合成一个完整的提示词提交给生成式AI；在得到 AI 工具输出的结果后，再从正确性、效率性、场景适配性等多个维度，对结果进行评价。根据评价结果，对提示词的表述逻辑或细节进行迭代优化，整个过程形成了一个完整的闭环。

简要回顾一下这部分提到的计算思维：第一类，解决问题的核心基础思维：问题拆解、抽象化（模式识别）、算法设计（不可忘记的递归算法）、模块化、调试（评估和指标）、迭代；第二类，从系统级考虑问题：并行、权衡、容错与恢复；第三类，近年来的技术演化：数据思维和提示词工程。而所有的这些思维，都可以在归结为一种思维：自动化工具——依靠工具（计算机科学中就是计算机，别的学科可以有别的工具）自动重复执行，减少人工操作，同时保障操作的标准化。别看如今人工智能来势汹汹，与其说是“智能”冲击，不如说是“自动化”冲击。以前需要靠人的事，现在人工智能可以做。所以这次技术浪潮才有那么大的影响。

在当前，计算思维的价值，往往体现两个方面：一是产品或解决方案，二是人才选拔，下面分别举例说明。

在产品方面，例如字节的推荐系统：

整个推荐系统的核心任务，是处理海量的用户行为数据、文章 / 视频内容数据，以及海量的用户更新请求流量，这一过程的底层逻辑，式计算思维在数据密集型场景下的落地；

在数据处理层，通过分布式的大数据处理集群，将海量的用户行为数据、文章 / 视频内容数据，进行并行的采集、清洗、格式化处理，以及在线 / 离线的分析计算；

在推荐逻辑层，它通过模块化的拆分设计，将整个推荐业务的逻辑流程，拆分为多个可独立扩容、独立升级的子服务，包括用户兴趣召回、候选内容召回、用户兴趣向量的实时计算、候选内容的实时过滤、推荐结果的混合排序、推荐结果的去重合并等多个模块化的子服务；

在流量调度层，它通过分片规则和负载均衡策略，将海量的用户更新请求流量，分发到多个应用服务器节点上并行处理；

同时，为了支撑海量数据下的高并发访问、低延迟响应的核心业务需求，在多个技术维度之间进行了细致的权衡，将热点数据存储在内存中，提升接口响应速度，同时在数据库层，也做了多级分片、多副本的容错方案。

整个架构的设计逻辑，是对计算思维中 “分解”“抽象”“模块化”“并行处理”“权衡思维”“容错与恢复” 等的综合落地应用。

在人才选拔方面，例如一道某厂面试题：设计一个短链服务，它可以将一个原始的长链接，转换为一个长度固定的短链；用户访问这个短链时，服务会自动将其重定向到对应的原始长链接；业务场景的规模是：每天生成的新短链数量为千万级，短链的跳转请求峰值为每秒千万级，要求短链服务的可用性不低于 99.99%，并且对跳转请求的响应延迟，不能超过 100 毫秒。

应用计算思维分析：

问题拆解：是否能将整个短链服务的设计问题，拆解为多个独立的、具备单一职责性的子服务，这些子服务包括：短链生成服务、短链跳转解析服务、短链存储服务、短链访问统计服务、服务状态监控服务等；

抽象化：是否能从短链服务的业务场景中，提取出核心的技术模型，这些技术模型包括：短链与原始长链接的映射关系、短链的生成规则、短链的访问过期时间规则、短链访问日志的存储结构等；

算法设计：是否能为短链生成的核心逻辑，选择合适的算法策略 —— 既要保证生成的短链字符串，具备足够的随机性，不能轻易被外部人员遍历获取，同时，也要保证短链生成过程的执行效率，不能在高并发场景下，出现大量的执行超时情况；

模块化：是否能将各个子服务的技术实现逻辑，以及子服务间的通信、协作的接口逻辑，组合成一个完整的技术方案；

并行处理：是否能在流量接入层，设计合理的负载均衡策略，将海量的用户跳转请求流量，分发到多个应用服务器节点上，并行地执行跳转逻辑；

缓存：是否能设计多层的缓存策略，将热点短链的映射关系，存储在离用户最近的边缘节点，或应用服务器的内存中，通过缓存的快速读取能力，大幅缩短请求的响应时间；

权衡思维：是否能在多个技术维度之间，进行合理的技术权衡 —— 例如，为了保证短链跳转服务的高可用，应该选择对数据进行多副本备份，还是对数据进行分片存储？为了提升接口的响应速度，应该选择将热点数据存储在内存中，还是直接存储在磁盘上？为了节省存储空间，应该选择将短链的映射关系数据，进行压缩后再存储，还是直接存储原始数据？

容错与恢复：是否能设计整个系统的高可用策略，包括服务的多副本部署、数据库的多副本复制、流量的自动故障切换等，保证在部分节点故障时，整个系统仍能正常提供服务。

最后咱们做个小结。

计算思维是计算机科学领域的底层认知逻辑。它的发展，是一个典型的 “实践 -> 理论 -> 实践” 的演化的过程。随着生成式人工智能的普及，普通人理解和掌握计算思维的重要性，被进一步提升。计算思维成为了普通人与人工智能高效协同的认知基础，提升人与人工智能工具协作的整体效率，解决更为复杂的问题。
