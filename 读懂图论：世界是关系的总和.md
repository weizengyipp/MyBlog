# 读懂图论：世界是关系的总和

## 引言

有这么一个问题：给定一个包含 N 个节点的通信网络，节点间的连接有不同带宽成本。请你设计一个算法找到连接所有节点的最小成本方案，并分析算法的时间复杂度。这是一道大厂面试题，其考察重点就是图论。

什么是图论？将现实世界中的实体抽象为节点，将实体间的关系抽象为边，从而将纷繁复杂的实际场景转化为可以精确计算的图结构，之后在这个结构化模型上求解最优或可行的特定方案。这便是图论。

任何系统并非由孤立要素构成，而是由要素之间的相互关系所定义。许多复杂问题的答案，不在个体本身，而藏在个体之间的连接之中。从 18 世纪的哥尼斯堡七桥问题，到今天社交网络的千亿级关系推荐，图论成为了计算机科学与工程实践中不可或缺的核心工具。

## 1 图论的理论发展

我们并非先独立存在，而后才建立连接；连接从一开始就内在于存在本身。事物的属性，很大程度由它所处的关系网络塑造。整体不只是部分之和。当你牵动世间一物，会发现它与万物紧密相连。交互与关系会涌现出个体不具备的新性质，这正是我们需要图这类结构工具去刻画复杂现实的根本原因。

### 1.1 起源：欧拉与图论的诞生

18 世纪的普鲁士哥尼斯堡城（今俄罗斯加里宁格勒），有一条穿城而过的普雷格尔河，河中有两座岛屿，两岸和岛屿之间共由七座桥梁连接。当地居民长期困惑于这样一个生活谜题：能否找到一条路径，从任意地点出发，恰好通过每座桥一次，再回到起点？这个在当时无人能解答的问题，直到 1736 年，瑞士数学家莱昂哈德・欧拉从数学层面给出了完整的否定性证明，并在解决这个问题的过程中，开创了图论。

欧拉将被河流分割的四块陆地抽象为四个节点，将连接陆地的七座桥梁抽象为七条边。哥尼斯堡七桥问题的图论表达是：在这个七桥的连通图中，是否存在一条能遍历所有边且仅遍历一次的闭合路径，即欧拉回路。

欧拉提出了判定一般图存在欧拉回路的充要条件：第一，图必须是连通的；第二，图中所有节点的度数必须是偶数。而哥尼斯堡七桥问题所对应的图，四个节点的度数均为奇数，因此不可能存在这样的闭合路径。

欧拉创立了 “用节点和边建模关系” 的图论思维范式，为后续图论的所有发展奠定了最基础的理论基石。但此后的近两百年里，图论一直停留在理论萌芽阶段，研究进展相对缓慢，主要被数学家们用来解决一些具象的游戏谜题或数学难题。其中最著名的当属英国数学家弗朗西斯・格思里在 1852 年提出的四色猜想：任何一张地图，都只需要用四种颜色着色，就能保证所有相邻区域的颜色互不重复。这一猜想直到 1976 年才被肯尼斯・阿佩尔和沃尔夫冈・哈肯借助计算机完成证明，成为图论发展史上的经典理论里程碑。

### 1.2 体系化：从理论到算法的突破

1936 年，匈牙利数学家德内什・柯尼希出版了图论历史上第一部完整的学术专著《有限与无限图的理论》。这部著作系统梳理了图论诞生以来两百年的所有零散研究成果，首次明确了图论的基础学术概念，厘清了基本定理间的逻辑关联，将图论从零散的问题研究整合成一门严谨的数学分支。这标志着图论正式完成理论体系化，成为一门具备完整研究范式的独立分支学科。

而真正让图论从数学理论走向计算机工程实践的关键契机，是 20 世纪中期电子计算机的诞生。计算机的高并发、海量数据处理能力，恰好满足了图论算法大规模计算的需求；而图论的关系建模特性，也恰好满足了计算机科学中网络、数据存储、程序优化等领域的建模需求。这一阶段出现的三大经典图论算法，到今天仍是图论工程应用的基础：

1.最短路径算法：1956 年，荷兰计算机科学家埃德斯・戴克斯特拉(Dijkstra)在亲身规划荷兰阿姆斯特丹至鹿特丹的公共交通路线时，首次直观展示了他亲手设计的最短路径算法；到 1959 年，他在学术论文《关于图的两个问题的注记》中正式公开了完整的算法细节。其逻辑是，通过贪心策略逐步迭代，找到带非负权值的图中从单一源节点到其他所有节点的最短路径。这一核心逻辑至今仍应用于 GPS 导航系统、互联网路由协议等场景。

```
// Dijkstra 算法伪代码
// 输入：图G(邻接表), start：源起点, n：节点总数
// dist[]：dist[v]表示起点到v的最短距离，初始无穷大INF
// visited[]：标记节点是否已确定最短路径
// prev[]：记录路径前驱节点，用于回溯完整路径
FUNCTION Dijkstra(G, start, n):
    // 初始化距离数组
    FOR v FROM 0 TO n-1:
        dist[v] = INF
        visited[v] = FALSE
        prev[v] = NULL
    END FOR
    dist[start] = 0   // 源点到自身距离为0

    FOR i FROM 0 TO n-1:
        // 步骤1：选出未访问、dist最小的节点u
        u = SelectMinDistNode(dist, visited, n)
        IF dist[u] == INF:
            BREAK  // 剩余节点不可达，提前退出
        END IF
        visited[u] = TRUE  // u的最短路径已经确定

        // 步骤2：松弛操作，遍历u所有邻接边 u→v，权重w
        FOR EACH (v, w) IN G[u]:
            IF NOT visited[v] AND dist[v] > dist[u] + w:
                dist[v] = dist[u] + w
                prev[v] = u
            END IF
        END FOR
    END FOR
    RETURN dist, prev
END FUNCTION

// 辅助函数：选出未访问距离最小节点
FUNCTION SelectMinDistNode(dist, visited, n):
    min_dist = INF
    u = -1
    FOR v FROM 0 TO n-1:
        IF NOT visited[v] AND dist[v] < min_dist:
            min_dist = dist[v]
            u = v
        END IF
    END FOR
    RETURN u
END FUNCTION

```

2.最小生成树算法：1956 年，美国数学家约瑟夫・克鲁斯卡尔提出最小生成树算法(Kruskal)；一年后的 1957 年，美国计算机科学家罗伯特・普里姆（Prim）也独立提出了另一种高效的最小生成树算法。这两类算法的目标是一致的：在带权无向图中找出一棵包含所有节点、且边权总和最小的生成树，但在实现策略上各有偏重：克鲁斯卡尔算法采用 “边贪心” 策略，优先选择全局权值最小的边，适合边数相对较少的稀疏图；普里姆算法则采用 “点贪心” 策略，从任一节点出发逐步扩展，优先连接当前生成树外部权值最小的边，适合节点间连接密集的稠密图。这两种算法的出现，满足了当时大规模通信网络、电力线传输等工程场景的优化需求，至今仍在相关领域广泛使用。


3.连通性与网络流算法：美国数学家小莱斯特・福特和德尔伯特・富尔克森在 1956 年提出了福特 - 富尔克森算法(Ford‑Fulkerson)，其逻辑是不断在残余网络中寻找增广路径（从源点 s 流向汇点 t 的一条可行路径），沿着这条路径推送尽可能多的流量；更新残余网络（正向边减去流量，反向边增加流量，反向边用于 “撤销” 流量，也就是退流）。反复迭代直到找不到任何增广路径，此时总流量就是最大流。这为后续解决最大流问题、建模网络流量提供了理论工具。

```
// FordFulkerson 算法伪代码
// 输入：图G(邻接表), s：源起点, t：汇点, n：节点总数
// 输出：最大流
FUNCTION FordFulkerson(G, s, t):
    max_flow = 0
    WHILE 存在一条增广路径 P 在残余网络 G 中从 s 到 t:
        // 求路径P上最小残余容量，即本次可推送流量
        augment = MinResidualCapacity(P)
        max_flow = max_flow + augment
        // 更新残余网络：正向边扣减，反向边增加
        UpdateResidualNetwork(G, P, augment)
    END WHILE
    RETURN max_flow
END FUNCTION

```

1972 年，美国计算机科学家罗伯特・塔扬发明了在有向图中寻找强连通分量的线性时间算法。这一算法能将海量节点的复杂有向图快速拆解为多个两两可达的独立子图，这一特性在互联网金融风控、社交网络群体识别等场景中是不可或缺的技术。后续出现的最大流算法，进一步补充了图论中关于网络流量分配、场景连通性的分析维度，让图论的工程适用性覆盖了更广泛的业务场景。


### 1.3 工程化：互联网时代的图论工程

20 世纪 90 年代后期，互联网的爆发式普及让全球数据量呈指数级增长，人们逐渐发现，现实中的网络结构既不是规则的点阵图，也不是厄多斯等人提出的完全随机图，真实网络的特征介于两者之间。这推动图论的发展进入了工程化阶段。其中最具代表性的研究成果，是 1998 年由美国康奈尔大学的邓肯・沃茨和史蒂文・斯特罗加茨提出的小世界网络模型，以及同年由谷歌创始人谢尔盖・布林和拉里・佩奇设计的 PageRank 算法，二者分别从结构建模和价值评估两个方向，为现代互联网的大规模应用奠定了技术基础。

小世界网络模型(Watts‑Strogatz，WS模型)，从规则环形近邻网络出发，以概率p对原有边进行随机重连，引入少量跨距离的 “捷径”。在较小重连概率区间，网络同时具备较高聚类系数与很短的平均最短路径，复现现实社会网络 “六度分隔” 的小世界效应。WS模型很好地解释了真实网络中 “高局部聚集性、短全局路径长度” 的特征。这一特征在社交网络中表现得最为直观：尽管用户的好友圈相对固定，属于典型的局部高聚集结构，但任意两个陌生人之间，平均也仅需要通过 6 个中间人就能建立连接，这正是典型的短全局路径特征。这一模型的出现，为后续社交网络的关系挖掘、传染病的传播路径分析、网络舆情的扩散规律研究，提供了完整的理论分析框架。

```
// Watts‑Strogatz 模型伪代码
// 输入：图G(邻接表), N：节点总数, k：每个节点初始近邻度数（偶数）
// p：重连概率 0 ≤ p ≤ 1
// 返回：邻接表 G

FUNCTION WattsStrogatz(N, k, p):
    G = INIT_EMPTY_ADJLIST(N)

    // --------步骤1：构建环形规则网络--------
    half_k = k / 2
    FOR u FROM 0 TO N‑1:
        FOR d FROM 1 TO half_k:
            v = (u + d) MOD N
            ADD_EDGE(G, u, v)   // 双向无向边
        END FOR
    END FOR

    // --------步骤2：遍历原有边，按概率p重连--------
    FOR u FROM 0 TO N‑1:
        // 只遍历右侧原始近邻，避免每条边处理两次
        FOR d FROM 1 TO half_k:
            v_old = (u + d) MOD N

            IF RandomFloat(0,1) < p:
                // 删除旧边 u <-> v_old
                REMOVE_EDGE(G, u, v_old)

                // 随机选一个新v_new，不能是u自己，不能已经相连
                v_new = RandomChoice(N)
                WHILE v_new == u OR IsEdgeExist(G, u, v_new):
                    v_new = RandomChoice(N)
                END WHILE

                ADD_EDGE(G, u, v_new)
            END IF
        END FOR
    END FOR

    RETURN G
END FUNCTION

// 辅助函数
PROCEDURE ADD_EDGE(G, u, v):
    APPEND v TO G[u]
    APPEND u TO G[v]
END PROCEDURE

PROCEDURE REMOVE_EDGE(G, u, v):
    DELETE v FROM G[u]
    DELETE u FROM G[v]
END PROCEDURE

FUNCTION IsEdgeExist(G, u, v):
    RETURN v IN G[u]
END FUNCTION

```

而布林和佩奇设计的 PageRank 算法，将整个互联网的海量网页建模为一张有向图：每个网页是一个节点，网页间的超链接关系是一条有向边。在这个基础上，PageRank 算法通过分析随机游走模型下网页的访问概率，来确定网页的权威度和价值权重。这解决了早期搜索引擎中网页价值评估不准、搜索结果相关性差的痛点，也让图论从纯粹的后端工程技术走向了海量用户级的前台应用场景。这一算法，也成为了所有主流搜索引擎排序算法的前身。

```
// PageRank 算法伪代码
// 输入：G 有向邻接表，N节点总数，d阻尼系数，epsilon收敛阈值，max_iter最大迭代次数
// 输出：网页价值权重数组 PR
FUNCTION PageRank(G, N, d=0.85, epsilon=1e-6, max_iter=100):
    // 初始化PR，所有网页均分权重
    PR = ARRAY(N)
    FOR i FROM 0 TO N‑1:
        PR[i] = 1.0 / N
    END FOR

    FOR iter FROM 1 TO max_iter:
        new_PR = ARRAY(N)
        // teleport基础项
        base = (1.0 - d) / N

        FOR i FROM 0 TO N‑1:
            sum_in = 0.0
            // 遍历所有指向i的前驱节点j (j → i)
            FOR j IN InNeighbors(G, i):
                out_deg_j = OutDegree(G, j)
                IF out_deg_j > 0:
                    sum_in = sum_in + PR[j] / out_deg_j
                END IF
            END FOR
            new_PR[i] = base + d * sum_in
        END FOR

        // 判断收敛：两次迭代总变化量小于阈值
        delta = ComputeL1Diff(PR, new_PR)
        PR = new_PR

        IF delta < epsilon:
            BREAK
        END IF
    END FOR
    RETURN PR
END FUNCTION

// 辅助：获取i的全部入邻接节点（所有j满足j→i）
FUNCTION InNeighbors(G, i):
    res = EMPTY LIST
    FOR j FROM 0 TO N‑1:
        IF i IN G[j]:
            APPEND j TO res
        END IF
    END FOR
    RETURN res
END FUNCTION

// 辅助：节点j出度
FUNCTION OutDegree(G, j):
    RETURN LEN(G[j])
END FUNCTION

// 辅助：L1总变化，计算两次PR差值绝对值之和
FUNCTION ComputeL1Diff(a, b):
    diff_sum = 0.0
    FOR idx FROM 0 TO LEN(a)-1:
        diff_sum = diff_sum + ABS(a[idx] - b[idx])
    END FOR
    RETURN diff_sum
END FUNCTION
```

2010年以来，随着大数据和人工智能技术的快速普及，工业界需要对百亿级甚至万亿级的实体关系进行实时存储、高效查询和深度分析，这又催生了图技术的工程化细分突破。在存储层面，为了应对海量关系的低延迟遍历需求，各科技巨头纷纷自研分布式图数据库，其中具有代表性的包括字节跳动的 ByteGraph、阿里的 AliGraph 等；在计算层面，为了让机器学习模型直接处理图结构的海量数据，图神经网络（GNN）也日趋完善。图论正从基础工程算法，进入到智能领域。

## 2 图论算法

在头部科技公司的算法面试中，由于贴近工程场景的建模，图论问题一直是核心考点。面试题和现实的工程问题类似，一般不会直接给出 “图” 的明确数据结构，而是需要候选人自己完成建模：识别清楚场景中的关键实体、实体间的关联关系，以及最终求解的目标类型；在此基础上，进一步完成算法选择、复杂度优化、边界条件处理的完整流程，这是中高级技术人员在实际工作中所需要具备的能力。

### 2.1 开篇问题：网络延迟优化与最小生成树

回到引言中那面试题，具体的场景是这样的：某数据中心需要在 N 台服务器之间建立通信链路，部分服务器间已经部署了专属的物理连接线路，且不同线路的带宽成本不同。现在需要设计一种方案，确保所有服务器都能直接或间接连通，且要求整个方案的总带宽成本最低。这是一个典型的大规模网络规划场景。

要使用图论解决这个问题，首先需要完成从实际场景到数学模型的抽象过程：
1. 节点建模：将每台服务器设备抽象为图中的一个唯一节点；
2. 边建模：将服务器之间的物理通信线路抽象为一条无向边，边的权值对应该线路的带宽成本；
3. 目标转化：问题的工程目标，转化为一个精准的图论目标：在这个无向连通图中，找到一棵包含所有节点，且边权总和最小的生成树。

完成建模后，这道题的图论表达是：最小生成树（MST）问题。

在明确了问题本质是求解最小生成树后，接下来需要根据数据规模、稀疏度等实际约束条件，选择匹配的算法实现方案。最小生成树有两种经典的经典解法，二者在策略、实现细节和适用场景上有明显差异：

Kruskal 算法：采用 “从小到大选择性地加入边” 的贪心策略。其逻辑是：先将图中所有边按照权值从小到大做升序排序；随后从权值最小的边开始依次选择，如果选中的边连接的两个节点目前不在同一个连通块内，就将这条边加入生成树；如果这两个节点已经在同一个连通块内，就跳过这条边；不断重复这一过程，直到生成树包含了图中所有的节点。这一算法需要使用并查集高效地检测两个节点是否属于同一连通块，以及快速合并两个连通块；为了保证在大规模数据场景下的执行效率，还需要对并查集进行路径压缩和按秩合并。Kruskal 算法的时间复杂度主要来自排序步骤，为 O (ElogE)，其中 E 为图中边的总数；它更适合处理节点数量较多、边数量较少的稀疏图场景。

```
// Kruskal 算法伪代码
// 输入：图G(邻接表), n：节点总数
// 输出：最小生成树边集合, 最小生成树权重总和
// 边结构体
STRUCT Edge:
    u      // 起点
    v      // 终点
    weight // 边权重

// Kruskal主函数
// G：全部边集合；n：节点总数
FUNCTION Kruskal(edges, n):
    mst_edges = EMPTY LIST   // 保存最小生成树选中的边
    total_weight = 0         // MST总权重

    // 1. 将所有边按照 weight 升序排序
    SORT edges BY weight ASC

    // 2. 初始化并查集
    DSU_Init(n)

    // 3. 从小到大遍历每一条边
    FOR e IN edges:
        u = e.u
        v = e.v
        w = e.weight
        // 判断u、v是否不在同一连通分量
        IF DSU_Find(u) != DSU_Find(v):
            DSU_Union(u, v)          // 合并两个集合
            APPEND e TO mst_edges
            total_weight = total_weight + w
            // MST已经集齐 n‑1 条边，可以提前结束
            IF LEN(mst_edges) == n - 1:
                BREAK
        END IF
    END FOR

    RETURN mst_edges, total_weight
END FUNCTION

// parent[] 父节点数组，rank[] 秩/深度数组
PROCEDURE DSU_Init(n):
    parent = ARRAY(0 … n‑1)
    rank = ARRAY(0 … n‑1)
    FOR i FROM 0 TO n‑1:
        parent[i] = i
        rank[i] = 1
    END FOR
END PROCEDURE

// 查找根，带路径压缩
FUNCTION DSU_Find(x):
    IF parent[x] != x:
        parent[x] = DSU_Find(parent[x])
    END IF
    RETURN parent[x]
END FUNCTION

// 按秩合并
PROCEDURE DSU_Union(x, y):
    root_x = DSU_Find(x)
    root_y = DSU_Find(y)
    IF root_x == root_y:
        RETURN
    END IF
    IF rank[root_x] < rank[root_y]:
        parent[root_x] = root_y
    ELSE:
        parent[root_y] = root_x
        IF rank[root_x] == rank[root_y]:
            rank[root_x] = rank[root_x] + 1
        END IF
    END IF
END PROCEDURE

```

Prim 算法：采用 “从点到面逐步扩展” 的贪心策略。具体实现逻辑是：从任意一个节点出发，先将该节点加入到生成树中；后续每一步迭代，都从当前生成树的所有候选边中，选择一条权值最小且连接到外部节点的边，将这条边和它连接的新节点加入到生成树中；不断重复这一过程，直到所有节点都被加入生成树。这一算法的时间复杂度取决于实现时采用的数据结构：如果使用邻接矩阵配合基础优先队列实现，时间复杂度为 O (V²)，其中 V 为图中节点的总数；如果使用邻接表配合二叉堆这类优先队列实现，时间复杂度可以优化到 O (ElogV)。Prim 算法更适合节点数量较少、边数量较多的稠密图场景，这是数据中心服务器集群布线中最常见的场景。

```
// 最小堆Prim 算法伪代码
// 输入：图G(邻接表), n：节点总数, start：源起点
// 输出：最小生成树权重集合, 最小生成树权重总和
// G：邻接表 G[u]存放 (v, weight)
FUNCTION PrimHeap(G, n, start):
    key = ARRAY(n)
    inMST = ARRAY(n)
    prev = ARRAY(n)
    FOR v FROM 0 TO n‑1:
        key[v] = INF
        inMST[v] = FALSE
        prev[v] = NULL
    END FOR

    key[start] = 0
    minHeap = MinHeap()
    minHeap.Push( (0, start) )
    total_weight = 0

    WHILE minHeap IS NOT EMPTY:
        (w_u, u) = minHeap.Pop()
        IF inMST[u]:
            CONTINUE
        END IF
        inMST[u] = TRUE
        total_weight = total_weight + w_u

        FOR EACH (v, w) IN G[u]:
            IF NOT inMST[v] AND w < key[v]:
                key[v] = w
                prev[v] = u
                minHeap.Push( (w, v) )
            END IF
        END FOR
    END WHILE
    RETURN total_weight, prev
END FUNCTION

```

在这道面试题的场景中，我们需要先向面试官确认服务器节点的数量，以及已部署线路的稀疏度，如果节点数量不多（比如小于 1000），优先选择 Prim 算法，代码逻辑会更简洁；如果节点数量非常多，且边的数量相对较少，选择 Kruskal 算法会更合适。

```
// 服务器集群布线问题伪代码
// ========== 步骤1：现实对象映射为图论模型 ==========
STRUCT Server:
    server_id       // 服务器唯一编号
    ip_addr         // 业务属性，不参与图计算

STRUCT CommLink:
    server_a_id     // 链路一端服务器
    server_b_id     // 链路另一端服务器
    bandwidth_cost  // 该通信线路带宽成本，边权

// 输入：服务器列表server_list，通信链路列表link_list
FUNCTION BuildServerGraph(server_list, link_list):
    // 1.节点映射：每一台服务器 → 图节点
    N = LEN(server_list)
    // 节点id与server_id一一对应
    node_id_map = DICT()
    FOR idx FROM 0 TO N‑1:
        s = server_list[idx]
        node_id_map[s.server_id] = idx
    END FOR

    // 2.边映射：物理通信线路 → 无向加权边
    edge_set = EMPTY LIST
    FOR link IN link_list:
        u = node_id_map[link.server_a_id]
        v = node_id_map[link.server_b_id]
        w = link.bandwidth_cost
        APPEND Edge(u, v, w) TO edge_set
    END FOR

    // 返回：图模型（全部边集合、节点总数），用于最小生成树求解
    RETURN edge_set, N, node_id_map
END FUNCTION

// ========== 步骤2：图论目标求解：求总权最小生成树 ==========
// 调用Kruskal最小生成树算法
edge_set, N, node_id_map = BuildServerGraph(server_list, link_list)
mst_edges, total_cost = Kruskal(edge_set, N)

// ========== 步骤3：模型结果反向映射回工程业务 ==========
FUNCTION MapMSTBackToBusiness(mst_edges, node_id_map):
    // 反向字典：图node_id → 原始server_id
    rev_map = INVERT_DICT(node_id_map)
    selected_links = EMPTY LIST
    FOR e IN mst_edges:
        s_a = rev_map[e.u]
        s_b = rev_map[e.v]
        cost = e.weight
        APPEND (s_a, s_b, cost) TO selected_links
    END FOR
    RETURN selected_links
END FUNCTION

// 输出结果：工程层面应当部署的通信线路集合、整体带宽总成本
deploy_links, total_bandwidth_cost = MapMSTBackToBusiness(mst_edges, node_id_map), total_cost

```

用图论解决实际工程问题，不是一上来就开始写具体算法代码，而是先完成从实际场景到图论模型的抽象建模过程，这是最核心的能力，也是应用图论的难点。接下来我们继续看看其他经常出现的考察点。

### 2.2 最短路径与网络规划问题

最短路径是图论中最经典的工业级问题，它的应用场景覆盖了所有需要在网络中寻找最优传输路径的需求，从日常的 GPS 导航，到内部服务的调用链优化，再到互联网上的数据包路由转发，本质都是在不同类型的图中寻找最短路径。根据边的权值特征、节点规模、路径目标范围，这类问题可以细分为三类典型场景，每类场景都有对应的最优算法：

单源最短路径：即从一个给定的源节点出发，计算到其他所有节点的最短路径。在边权值为非负的场景下，比如正常的导航路线规划中，道路的长度、通行时间不可能是负数，Dijkstra 算法是这类问题的标准解法；在实际工程代码中，一般会通过优先队列这一数据结构来进一步优化算法的执行效率，将时间复杂度从 O (V²) 优化到 O (ElogV)。而在某些特殊场景中，比如网络传输中存在负权边的情况，即部分线路传输成本为负，则需要使用 Bellman-Ford 算法，其基于松弛操作，对全部边执行最多 n-1 轮迭代；再执行一轮额外松弛判断负环。

多源最短路径：即计算图中每一对节点之间的最短路径。当业务场景需要获取全量节点对的最短路径时，通常会选择 Floyd-Warshall 算法。这一算法采用动态规划策略，通过三重循环逐步迭代，计算任意两个节点间的最短路径；代码非常简洁，仅需要一个三重循环即可完成，但时间复杂度为 O (V³)，因此只适合在节点数量规模较小的场景下使用。

```
// 输入：n节点数；graph[n][n]邻接矩阵
// graph[i][j]：i→j边权重；i==j为0；无边赋值INF
// dist[n][n]：保存任意两点最短距离
// next[n][n]：路径后继矩阵，用于还原完整路径
FUNCTION FloydWarshall(graph, n):
    INF = BIG_NUMBER

    // 初始化距离矩阵、后继矩阵
    dist = COPY_MATRIX(graph)
    next = INIT_MATRIX(n, n)
    FOR i FROM 0 TO n‑1:
        FOR j FROM 0 TO n‑1:
            IF graph[i][j] != INF:
                next[i][j] = j
            ELSE:
                next[i][j] = NULL
            END IF
        END FOR
    END FOR

    // k：中转节点；i起点；j终点
    FOR k FROM 0 TO n‑1:
        FOR i FROM 0 TO n‑1:
            FOR j FROM 0 TO n‑1:
                IF dist[i][k] != INF AND dist[k][j] != INF:
                    IF dist[i][j] > dist[i][k] + dist[k][j]:
                        dist[i][j] = dist[i][k] + dist[k][j]
                        next[i][j] = next[i][k]
                    END IF
                END IF
            END FOR
        END FOR
    END FOR

    // 检测负权环：i到i距离小于0，说明i处在负权环上
    has_negative_cycle = FALSE
    FOR i FROM 0 TO n‑1:
        IF dist[i][i] < 0:
            has_negative_cycle = TRUE
        END IF
    END FOR

    RETURN dist, next, has_negative_cycle
END FUNCTION

// 辅助：根据next后继矩阵，还原i到j完整路径
FUNCTION GetPath(next, i, j):
    path = EMPTY LIST
    IF next[i][j] == NULL:
        RETURN path   // 不可达
    END IF
    cur = i
    APPEND cur TO path
    WHILE cur != j:
        cur = next[cur][j]
        APPEND cur TO path
    END WHILE
    RETURN path
END FUNCTION
```

无向无权图的最短路径：这类场景是工业级最短路径问题中的一个特殊子集，比如在社交网络中，计算两个用户间的好友关系链长度，或是在通信网络中，计算两个节点间的最少跳转次数。由于这类场景中所有边的权值都相同，一般会选择广度优先搜索（BFS）来遍历整个图，以源节点为中心，逐层向外扩展，第一次到达目标节点的路径就是最短路径；这一算法的时间复杂度为 O (V+E)，是这类场景下的最优选择。

```
// 广度优先搜索（BFS）伪代码
// G：邻接表；n节点总数；s起始节点
// visited[]：标记是否访问；dist[]记录起点到各点距离
FUNCTION BFS(G, n, s):
    INF = BIG_NUMBER
    visited = ARRAY(n)
    dist = ARRAY(n)
    parent = ARRAY(n)   // 前驱，用于还原路径

    FOR i FROM 0 TO n‑1:
        visited[i] = FALSE
        dist[i] = INF
        parent[i] = NULL
    END FOR

    queue = Queue()
    // 初始化起点
    visited[s] = TRUE
    dist[s] = 0
    queue.PUSH(s)

    WHILE queue IS NOT EMPTY:
        u = queue.POP_FRONT()   // 队首出队

        // 遍历u所有邻接节点
        FOR v IN G[u]:
            IF NOT visited[v]:
                visited[v] = TRUE
                dist[v] = dist[u] + 1
                parent[v] = u
                queue.PUSH(v)
            END IF
        END FOR
    END WHILE

    RETURN visited, dist, parent
END FUNCTION

// 根据parent前驱数组，还原 s → t 的路径
FUNCTION ReconstructPath(parent, s, t):
    path = EMPTY LIST
    IF parent[t] == NULL AND t != s:
        RETURN path   // 不可达
    END IF
    cur = t
    WHILE cur != NULL:
        APPEND cur TO path
        cur = parent[cur]
    END WHILE
    REVERSE(path)
    RETURN path
END FUNCTION
```

在实际的业务场景中，根据问题建模和场景的约束条件，通常需要将这几类问题的解法组合起来才能解决问题，而不是靠记住标准解法。

### 2.3 拓扑排序与任务调度问题

拓扑排序是针对有向无环图（DAG）的核心算法，将图中的所有节点线性排序，以满足所有有向边所代表的前置依赖关系。这类问题的典型应用场景是工程中的任务调度类需求，比如在软件开发中，管理各个模块的编译依赖顺序；在大数据处理中，保证分布式任务的执行顺序符合数据依赖关系；在电商系统中，保障交易流程中的业务节点的依赖关系。

在工业级场景中，拓扑排序最常用的实现算法是 Kahn 算法，它的逻辑是基于节点入度的广度优先搜索：先遍历整个图，统计每个节点的入度；随后将所有入度为 0 的节点，即没有任何前置依赖的节点，加入到一个先进先出的队列中；接下来从队列中依次取出节点，将其加入到最终的拓扑排序结果中，同时将该节点的所有邻居节点的入度减 1，如果某个邻居节点的入度在减 1 后变成 0，说明它的所有前置依赖都已经被满足，便将其加入到队列中；不断重复这一过程，直到队列为空。在这个遍历过程中，如果存在无法进入队列的节点，就说明图中存在环路依赖，无法进行拓扑排序；反之，如果所有节点都能被加入队列，那么最终得到的节点序列就是一个合法的拓扑排序结果。 Kahn 算法的执行过程天然适合分布式并行执行，且在执行过程中就能实时检测环路的存在，非常适合处理大规模的任务调度场景。

```
// Kahn 算法伪代码
// G：有向图邻接表；n节点总数
// 返回：topo_order拓扑序列；has_cycle是否存在环
FUNCTION Kahn(G, n):
    in_degree = ARRAY(n)
    // 初始化入度数组
    FOR i FROM 0 TO n‑1:
        in_degree[i] = 0
    END FOR

    FOR u FROM 0 TO n‑1:
        FOR v IN G[u]:
            in_degree[v] = in_degree[v] + 1
        END FOR
    END FOR

    queue = Queue()
    // 入度为0的节点入队
    FOR i FROM 0 TO n‑1:
        IF in_degree[i] == 0:
            queue.PUSH(i)
        END IF
    END FOR

    topo_order = EMPTY LIST

    WHILE queue IS NOT EMPTY:
        u = queue.POP_FRONT()
        APPEND u TO topo_order

        // 遍历u的后继，入度‑1
        FOR v IN G[u]:
            in_degree[v] = in_degree[v] - 1
            IF in_degree[v] == 0:
                queue.PUSH(v)
            END IF
        END FOR
    END WHILE

    // 拓扑序列长度不等于节点总数，说明存在环路
    has_cycle = ( LEN(topo_order) != n )
    RETURN topo_order, has_cycle
END FUNCTION
```

### 2.4 连通性与聚类问题

连通性是图最基础的结构特征，将图中的所有节点划分为不同的连通分量；在同一个连通分量中，任意两个节点之间都存在至少一条可达路径；而不同连通分量之间的节点，完全没有可达路径。这类问题在工业级场景中最典型的应用是社交网络中的圈子聚类，金融风控中的失联用户关系挖掘，以及推荐系统中的用户群体分组。

在处理这类问题时，算法选择的关键依据是图的类型是有向图还是无向图：

对于无向图的连通性判断，最常用的算法是深度优先搜索（DFS）或广度优先搜索（BFS）。这两种算法都可以在线性时间复杂度内遍历完整个图，标记出所有连通分量，且实现逻辑简单、不易出错。

对于有向图的连通性判断，逻辑则要复杂得多：需要将连通性细分为强连通和弱连通两类。其中强连通分量的定义是：在该分量内的任意两个节点，都存在一条可以互相到达的有向路径。而求解强连通分量的经典算法，是 Tarjan 算法和 Kosaraju 算法；这两类算法都可以在线性时间内完成遍历，将一个有向图拆分为多个强连通分量。

弱连通是条件更为宽松的一类连通定义：将有向图中全部边的方向抹去，将其视作一张无向图，如果该无向图是连通的，则原有的向图为弱连通图。弱连通不要求节点之间存在双向的有向可达路径。对应的弱连通分量，就是把原图去掉边方向之后得到的无向图的连通分量。求解弱连通分量的做法是复制图结构、丢弃边方向转换为无向图，再运行普通 DFS/BFS 得到连通块，时间复杂度同样为 O (V+E)。

介于强连通与弱连通之间还有单向连通：图中任意一对节点，至少存在一个方向的有向可达路径（u 可以到达 v，或者 v 可以到达 u）。三者是包含关系：强连通 ⊂ 单向连通 ⊂ 弱连通。

在工程场景中，连通性往往不会独立作为核心逻辑使用，而是作为更复杂的图挖掘的预处理逻辑。三者有明确用途：强连通分量多用于识别闭环关系，例如风控里互相转账的团伙账户；弱连通适合粗粒度聚类，例如社交网络的关注关系、服务单向调用的调用链，只关心实体是否归属同一个大网络，不强调双向可达；单向连通则在任务依赖、链路传播类分析中会用到。

## 3 图论在工程中的应用

如前所述，从 2010 年进入大数据时代开始，由于天然适合对大规模关系数据进行存储、实时查询与深度分析，图论及相关技术，逐渐成为关键支撑技术。

### 3.1 社交网络与好友推荐

在社交网络类业务中，无论是陌生人推荐、好友推荐还是关注推荐，其底层业务逻辑都是在海量用户关系图中进行多跳邻居节点查询。其核心要求是：在数十亿级节点、万亿级边的超大规模图上，实时完成多跳遍历查询，且整个操作的延迟必须控制在用户无感知的范围内。为了满足这一严苛的性能要求，头部公司往往会选择基于图论的技术框架，自研对应的分布式图数据库引擎。

以字节跳动的 ByteGraph 为例，这是一个完全自研的分布式图数据库系统，其设计目标是支撑抖音、头条等上层社交类业务的海量实时图查询需求。ByteGraph 采用了有向属性图的数据模型，这是业界图数据库的标准模型之一。在这个模型中，图中的每个节点和每条边都可以自定义多个不同类型的属性。这一建模特性，天然匹配了社交网络场景中用户实体和关系的多维度属性存储需求。

例如，抖音社交网络中 “好友的好友” 这类多跳关系的实时挖掘，是抖音好友推荐的核心逻辑之一。在抖音的社交关系图中，每个用户是一个节点，用户间的关注、粉丝关系是一条有向边；而好友推荐的核心业务逻辑，正是基于用户的多跳邻居节点遍历，来挖掘潜在的好友关系。在这一场景中，ByteGraph 支撑了大量的多跳查询请求，比如查询某一用户的关注对象的粉丝，或是查询其粉丝的关注对象。

截至 2026 年，ByteGraph 已经在字节跳动内部部署了超过 1000 个集群，分布在全球多个机房，支撑了包括抖音、头条、西瓜视频等几乎所有核心业务的图数据存储和查询需求。而其整个集群的读写吞吐能力可以弹性扩展到每秒千万级查询，在线处理用户提交的两跳或三跳邻居查询请求时，P90 延迟（即 90% 的请求响应时间）可以控制在 20 毫秒以内。

### 3.2 推荐系统：从协同过滤到图神经网络

推荐系统是图技术在工业界最核心的落地场景之一：通过用户的历史行为数据、用户画像属性和商品相关属性，来预测用户可能感兴趣的其他内容，比如商品、短视频、文章等。这是一个典型的关系预测问题，技术难点是，如何高效捕捉用户与推荐对象之间的复杂高阶关联关系。图结构的天然数据建模，能精准地描述和挖掘这类高阶关系。

在传统推荐系统中，最常用的算法是协同过滤，其逻辑是，基于用户的历史行为数据来计算用户或商品的相似度，进而生成推荐结果。这类算法在实际场景中，存在着两个技术痛点：一是数据稀疏性问题，即平台中大部分用户的历史行为数据相对较少，导致用户或商品的相似度计算结果不准；二是冷启动问题，即新用户或新商品由于缺乏足够的历史行为数据，无法被有效的推荐给用户。

而图技术正好可以解决这两个技术痛点：将用户、商品、商品属性等不同类型的实体都建模为节点，将用户行为、商品属性关联等不同类型的关系建模为边，然后通过图神经网络的消息传递机制，捕捉用户与商品之间的高阶间接关联 —— 这类高阶关联关系，是传统协同过滤算法无法有效捕捉的信息。

在推荐业务工程化场景中，头部公司基本都选择了 “图数据库存储 + 图神经网络计算” 的技术架构。例如阿里巴巴的 EGES（Enhanced Graph Embedding with Side Information）模型。这一模型的设计目标，是解决淘宝推荐业务中的严重数据稀疏性和冷启动问题：淘宝的商品库规模达到了数十亿级，大部分商品的用户交互数据都很少，属于典型的长尾商品；同时，每天还有大量的新商品上架，这些新商品几乎没有任何用户交互数据。

EGES 模型的思路，是构建一个包含多类型节点和边的异构图。在这个异构图中，用户、商品、商品的品牌和类别等属性都被建模为不同类型的节点；而用户的点击、加购、购买、收藏等不同类型的行为，以及商品间同品牌、同类别、经常一起购买的关联关系，都被建模为不同类型的边；同时，技术团队还为不同类型的边，设置了不同的权重值，比如用户的点击行为权重为 1，加购行为权重为 3，购买行为权重为 5，收藏行为权重为 4。在这个基础上，EGES 模型通过带重启的随机游走策略，在这个海量异构图上进行多路径采样，学习商品的序列表示特征；再通过图神经网络的消息传递机制，将商品的属性特征融合到节点表示中。

```
// EGES 模型伪代码
// 输入：
// G：商品有向加权图；S：每个节点对应的side‑info集合
// walks_per_node：每个节点随机游走次数；walk_len：游走序列长度
// window：skip‑gram窗口；ns：负采样数；d：embedding维度
// n_side：侧信息类型数量；输出 W0...Wn（emb矩阵），A权重矩阵

FUNCTION EGES(G, S, walks_per_node, walk_len, window, ns, d, n_side):
    N = NUM_NODES(G)
    // W0：物品id embedding；W1~Wn：各类side‑info embedding
    W = ARRAY(n_side + 1)
    FOR j FROM 0 TO n_side:
        W[j] = RandomInitMatrix(N, d)
    END FOR
    // A：权重矩阵[N × (n_side+1)]，a_v^j，可学习参数
    A = RandomInitMatrix(N, n_side + 1)

    FOR iter FROM 1 TO walks_per_node:
        SHUFFLE(G.V)
        FOR v IN G.V:
            // 加权随机游走，生成商品序列SEQ
            SEQ = WeightedRandomWalk(G, v, walk_len)
            // 加权Skip‑Gram训练，更新W、A
            WeightedSkipGram(W, A, S, SEQ, window, ns)
        END FOR
    END FOR
    RETURN W, A
END FUNCTION

// 加权随机游走：按边权重采样下一跳
FUNCTION WeightedRandomWalk(G, start, walk_len):
    seq = EMPTY LIST
    cur = start
    APPEND cur TO seq
    FOR t FROM 1 TO walk_len‑1:
        neighbors = G[cur]
        IF LEN(neighbors) == 0:
            BREAK
        END IF
        // 根据边权重做概率采样
        cur = WeightedSample(neighbors)
        APPEND cur TO seq
    END FOR
    RETURN seq
END FUNCTION

// WeightedSkipGram 核心：EGES的skip‑gram，区别普通deepwalk
FUNCTION WeightedSkipGram(W, A, S, SEQ, window, ns):
    FOR pos FROM 0 TO LEN(SEQ)-1:
        v = SEQ[pos]
        // 窗口内上下文
        context_list = GetWindowContext(SEQ, pos, window)
        FOR u IN context_list:
            // -------- 计算物品v融合后的向量Hv --------
            numerator = ZERO_VECTOR(d)
            denominator = 0.0
            FOR j FROM 0 TO n_side:
                a = A[v][j]
                exp_a = EXP(a)
                numerator = numerator + exp_a * W[j][v]
                denominator = denominator + exp_a
            END FOR
            Hv = numerator / denominator

            // 负采样，采样ns个负样本
            positive_loss = SkipGramUpdate(Hv, W[0][u], label=1)
            FOR k FROM 1 TO ns:
                neg_node = NegativeSample()
                SkipGramUpdate(Hv, W[0][neg_node], label=0)
            END FOR
        END FOR
    END FOR
END FUNCTION

// SkipGramUpdate：sigmoid‑binary cross‑loss梯度更新W、A
PROCEDURE SkipGramUpdate(Hv, zu, label):
    // loss = −[ label·logσ(Hv^T zu)+(1‑label)log(1−σ(Hv^T zu)) ]
    // 反向传播更新：zu，W[j][v]，A[v][j]
    // 梯度按论文公式(9)(10)(11)更新参数
END PROCEDURE

// 推理阶段：得到商品最终向量
FUNCTION GetItemEmbedding(v, W, A, n_side):
    numerator = ZERO_VECTOR(d)
    denominator = 0.0
    FOR j FROM 0 TO n_side:
        exp_a = EXP(A[v][j])
        numerator = numerator + exp_a * W[j][v]
        denominator = denominator + exp_a
    END FOR
    Hv = numerator / denominator
    RETURN Hv
END FUNCTION
```

虽然长尾商品和新商品，虽然没有足够的用户交互数据，但有丰富的品牌、类别等属性节点关系，这一方案通过这些属性节点的信息传递，模型可以精准学习这些商品的特征表示，有效提升这类商品在推荐结果中的曝光比例。

### 3.3 深度学习：图神经网络的应用

在人工智能领域，图论是目前唯一可以系统性对实体间复杂关系进行建模和特征挖掘的数学基础。例如深度学习中，卷积神经网络（CNN）和循环神经网络（RNN）等传统模型，只能处理欧式结构(即欧几里得空间)的数据，比如图像、音频、文本这类在规则网格或线性空间中存储的数据。但在实际业务场景中，更多重要的数据类型是非欧式结构的图数据。这类数据无法用传统的二维表格或一维序列结构来有效描述。而图神经网络（GNN）将深度学习技术，从传统的欧式空间数据处理，扩展到了非欧式空间的图数据处理。

在实际业务场景中，图神经网络的应用，必须建立在一个高性能的分布式图系统之上。这类系统需要同时支撑高效的图存储、图采样和图计算能力，才能在超大规模图数据的场景下，完成 GNN 模型的训练和推理。其中具有代表性的，是阿里巴巴的 AliGraph 平台。这是一个专门为图神经网络场景设计的分布式图平台，支撑图神经网络从底层数据存储到上层模型计算的全链路流程。在前述的淘宝推荐场景中，它支撑了 GNN 模型的高效训练，相比传统的基于矩阵分解的推荐模型，训练速度提升了 40% 到 50%；同时，它还支撑了阿里内部的其他多种业务场景，包括商品的相似性推荐、用户的社交关系挖掘等。

除此之外，图论还在其他人工智能领域中得到了广泛应用：

计算机视觉：在场景图生成任务中，图技术可以识别图像中的关键实体，比如物体、人物等，以及实体间的语义关系，比如 “在… 左侧”“拿着”“坐在… 上面”，再将这些信息转化为图结构；这一结构，可以进一步支撑图像检索、场景理解等高级任务。

自然语言处理：在语义解析任务中，图技术可以将句子中的语义关系和语法结构，转化为抽象的图结构，用来做实体级的语义消歧，提升模型对文本的理解深度。

机器学习的特征工程：在用户风险识别、商品反作弊等场景中，图技术可以将实体间的关联关系，作为特征输入到模型中，让模型学习到更丰富的结构特征，显著提升识别的精准度。

## 3.4、跨学科研究中的作用

图论的建模和分析能力，如今已经成为跨学科研究的标准底层工具。在这一领域，最具代表性的工业级应用场景，是药物的发现和开发流程。

传统的药物发现流程，是在实验室中对海量候选分子进行逐一测试，筛选出具有目标生物活性的候选分子，再进行后续的临床前和临床实验；这一流程的成本极高，单个化合物的相关实验成本可以达到 10 万美元，而且周期极长，从分子筛选到最终完成临床实验，往往需要数年的时间。而在这一场景中引入图技术后，可以在分子筛选阶段，先通过机器学习模型对海量候选分子进行虚拟筛选，预测其核心分子属性，大幅减少了需要进行实际实验的分子数量，显著降低了研发成本和研发周期。

这一技术方案的逻辑，是将药物分子的结构模型，建模为一张标准的图结构：其中分子的每个原子，是一个节点；原子间的每个化学键，是一条边。在这个基础上，研究人员可以使用图神经网络的消息传递机制，来学习分子的结构特征；将分子的结构特征和其物理化学性质，整合为完整的图级表示，输入到属性预测模型中进行训练，最终可以精准预测分子的关键属性，比如分子的稳定性、溶解度，或是与特定蛋白质靶点的结合亲和力。

```
// ========= 分子‑图结构抽象建模 =========
// 原子结构体
STRUCT Atom
    atom_id        // 原子唯一编号，图节点ID
    element_type   // 元素类型：C、H、O、N等，节点属性
    charge         // 原子电荷，节点附加特征
END STRUCT

// 化学键结构体
STRUCT Bond
    atom_u_id      // 化学键一端原子id
    atom_v_id      // 化学键另一端原子id
    bond_order     // 键级：1单键，2双键，3三键；作为边权
END STRUCT

// 输入：原子列表atom_list，化学键列表bond_list
// 输出：图邻接表G；节点特征node_features；边权edge_weight
FUNCTION MoleculeToGraph(atom_list, bond_list):
    N = LEN(atom_list)
    // 原子id映射到图内部节点索引
    node_map = DICT()
    FOR idx FROM 0 TO N‑1:
        atom = atom_list[idx]
        node_map[atom.atom_id] = idx
    END FOR

    // 构建邻接表
    G = INIT_EMPTY_ADJLIST(N)
    edge_weight = EMPTY DICT   // key:(u,v) value:键级

    FOR bond IN bond_list:
        u = node_map[bond.atom_u_id]
        v = node_map[bond.atom_v_id]
        w = bond.bond_order

        ADD_EDGE(G, u, v)
        ADD_EDGE(G, v, u)
        edge_weight[(u, v)] = w
        edge_weight[(v, u)] = w
    END FOR

    // 节点特征矩阵：每个节点保存元素、电荷等属性
    node_features = ARRAY(N)
    FOR idx FROM 0 TO N‑1:
        atom = atom_list[idx]
        node_features[idx] = (atom.element_type, atom.charge)
    END FOR

    RETURN G, node_features, edge_weight, node_map
END FUNCTION

// 反向映射：图计算结果映射回原始分子对象
FUNCTION GraphBackToMolecule(result_graph, node_map):
    rev_node_map = INVERT_DICT(node_map)
    ...
    RETURN molecule_result
END FUNCTION

// 辅助函数：无向边加入邻接表
PROCEDURE ADD_EDGE(G, u, v):
    APPEND v TO G[u]
END PROCEDURE
```

某跨国药企在 COVID-19 特效药研发过程中采用的 MP-GNN 模型（Message‑Passing Neural Network，消息传递图神经网络），该模型分为消息传递迭代阶段与读出阶段：在消息传递阶段，每个节点接收来自邻居的消息，经聚合与更新迭代，不断学习融合拓扑与边特征；读出阶段聚合全部节点向量，输出整张分子图的表征。将药物分子抽象为原子‑化学键图后，MPNN 可端到端完成分子毒性、溶解度等属性预测，不需要人工设计分子指纹特征。

```
// MP-GNN 模型
// 分子图输入
// G：邻接表；node_feat：原子节点特征矩阵；edge_feat：边(化学键)特征；T：消息传递层数
// M_t：消息函数(MLP)；U_t：节点更新函数(MLP/GRU)；R：Readout读出函数
STRUCT MPNN_Model
    T                  // 消息传递层数
    M_list             // 每层消息函数 M_1 … M_T
    U_list             // 每层更新函数 U_1 … U_T
    Readout R          // 图读出函数
END STRUCT

// 前向推理
FUNCTION MPNN_Forward(model, G, node_feat, edge_feat):
    N = LEN(node_feat)
    // 初始化节点隐藏状态 h_v^(0)
    h = COPY(node_feat)

    FOR t FROM 0 TO model.T‑1:
        M_t = model.M_list[t]
        U_t = model.U_list[t]

        m = ARRAY(N)   // m[v]：节点v聚合得到的消息向量，初始全0
        FOR v FROM 0 TO N‑1:
            FOR u IN G[v]:   // u是v的邻居
                e_uv = edge_feat[(v, u)]
                msg = M_t(h[v], h[u], e_uv)
                m[v] = AGGREGATE(m[v], msg)   // AGG：sum/mean/max
            END FOR
        END FOR

        // 更新节点隐藏状态
        FOR v FROM 0 TO N‑1:
            h[v] = U_t(h[v], m[v])
        END FOR
    END FOR

    // Readout：聚合全部节点得到整张分子图表示，用于分子性质预测
    graph_embedding = model.Readout(h)
    RETURN graph_embedding, h
END FUNCTION

// AGGREGATE 聚合示例：求和
FUNCTION AGGREGATE(a, b):
    RETURN a + b
END FUNCTION
```

在疫情爆发初期，该药企的传统高通量筛选方案，评估每一万个候选药物分子需要数月的时间；而在改用 MP-GNN 模型后，通过多尺度图表示学习技术，将分子的原子级图结构、官能团子图结构、以及其他物理化学性质的特征整合到一起，将评估每一万个候选分子的时间从数月压缩到了 72 小时，实现了对候选分子属性的精准预测，大幅缩短了特效药的研发周期。


## 4 小结

我们习惯于关注事物本身，但世界运行的秘密，往往写在事物与事物的关系里。现实世界大量难题，本质就是一张纠缠的关系之网，建模关系，才是求解问题的前提。图论走过了近三百年的发展历程，其核心是：将真实世界中的复杂关系问题，抽象为标准化的图结构模型，进而通过成熟的算法或工程方案，系统性地求解其最优解。
