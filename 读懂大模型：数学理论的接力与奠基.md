# 读懂大模型：数学理论的接力与奠基

大模型有两个核心过程：训练和推理。训练过程是对数据中存在的通用模式的有效压缩，推理过程则是对未见过的序列进行最优归纳预测。这背后是一场数学理论的接力和奠基。现在就让我们回到人工智能的原点。(提醒：后文涉及一些数学理论，不理解跳过不看，不影响整体理解。)

1956 年被公认为人工智能的元年，标志是达特茅斯会议。这一年，逻辑学家约翰・麦卡锡作为达特茅斯会议的主导组织者，首次提出“人工智能”这个词，作为一个独立的正式研究领域。据约翰・麦卡锡后来回忆，“人工智能”这个词并非他的原创，而是从别人那听来的。谁是原创者，似乎已无从考证。

在会议期间，约翰・麦卡锡向理论计算机科学家雷・所罗门诺夫提了这么一个思想实验：假设你走进一间废弃老房子，屋里摆着一台打印式图灵机，它正在持续打印一串字符序列。你恰好赶到时，它已经打出一大段已知文本，马上要打印下一个字符。现在让你下注：你赌它接下来会打出什么字符？

这个思想实验，再简化一点：假设有一个运行在图灵机上的函数，可以观察到函数的一堆输出，能否从这些输出推出下一个输出是什么。暗含的是从一堆输出中，推出这个函数是什么。

约翰・麦卡锡的观点是所有数学推理、智能问题，本质都是「图灵机求逆」： 一台图灵机输入程序 P，输出序列 S；求逆就是：已知输出片段 S₁S₂…Sₙ，反推最可能生成它的程序 P，再预测下一个输出字符。

所罗门诺夫则认为这就是他一直想解决的归纳问题——序列续写（sequence continuation）： 给你一段文字 / 数字的开头，判断接下来会出现什么，靠概率猜测规律，这就是人类所有学习、预测行为。

从我们今天的角度来理解：

“打印式图灵机”，就是任意一台能够生成数据的图灵机，从广义的计算理论看，世界本身、自然规律、人类语言都是一台 “打印机”；而对大模型训练就是寻找图灵机上运行的函数；

“已经打印出的一大段已知文本”，就是我们观测到的全部历史数据、过往经验等等，对应着训练大模型使用的海量数据；

“猜下一个打印字符”，就是现在大模型的Next Token Prediction，预测下一词元，当时他们所用的词还是next word prediction。

在此次会议论文集《自动机研究》中，麦卡锡在他的论文《图灵机定义的逆函数》中，将这个思想实验做了严格的数学定义：“图灵机求逆”：给定一个通用图灵机 TM，以及其对应的一个递归函数 f，还有该函数的已知输出结果 y；找到满足条件 f (x)=y 的对应输入 x。

麦卡锡工作的突破性在于，将 “机器实现智能” 的行为逻辑，从单纯的 “输入→输出” 的函数计算过程，重新构建为 “反向匹配有效输入” 的逆向搜索过程。这恰好符合大模型的核心任务逻辑：无论是对话、代码生成还是摘要，大模型都需要根据用户给定的前文，相当于 “函数输出的约束条件”，反推后续最符合约束条件的文本序列。

这一问题提出后，时间直接来到了1964年，经过八年的努力，雷·所罗门诺夫发表了论文《 a fromal theory of inductive inference》，从数学上证明了，麦卡锡的图灵机求逆等价于他的next token prediction，并提出通用归纳理论，设计了一种理论上的通用预测机制，为序列预测提供了数学上最优解的思路：

在所有能解释观测数据的模型中，越简单的模型，即对应图灵机程序长度越短的模型，赋予的后验概率权重越高；如果算力和数据资源不受限制，通过对所有符合条件的程序，按照 2^(-|p|) 的概率分布做加权平均，结果就会收敛到理论上的最优预测值。

这一机制被正式命名为 “所罗门诺夫通用预测器”，其技术逻辑可以更严谨地拆解为两个核心技术步骤：

全空间程序搜索：遍历通用图灵机上所有可能生成观测数据序列的合法停机程序，这一过程在理论上需要覆盖无限长的程序空间；

概率加权预测：对所有这些候选程序，以 “程序长度的指数倒数” 为基准进行加权平均；再根据已有的观测数据做条件概率过滤，最终将这个加权平均的结果作为下一个序列元素的预测输出。

值得注意的是，所罗门诺夫归纳过程：观察数据；形成一个假设来解释（生成）数据；验证假说；修正假说。所罗门诺夫认为，人类、通用智能只能依靠有限观测做概率猜测；“猜打印机下一个字符” 是智能最基础、最通用的底层行为，也就是最广义的学习。也就是说图灵机求逆，next token prediction 就是最广义的学习。这是大模型能够学习的数学基础支撑。

所罗门诺夫设计的归纳过程本身，在“最优预测的准确性” 与 “程序长度”问题，即“数据压缩的有效性”问题之间建立了逻辑等价联系：能准确预测序列的模型，也就是长度最短的程序，也必然是能高效压缩该序列的模型。

但在所罗门诺夫的归纳理论中，缺少一个关键的理论支撑：如何严格定义一个模型的 “简洁性”？或者说，如何度量一个程序的最短长度？如何衡量数据压缩有效性呢？这就需要算法信息论的支持了。

在继续之前，可以暂停几秒思考一下，什么是“随机”，什么是“压缩”。我们经常使用随机数、经常说压缩数据，那什么是“随机”，什么是“压缩”呢？其实，在很长的一段时间里，科学家和我们一样，对此都是直觉性地使用，而没有严格的定义。直到前苏联数学家安德雷・柯尔莫哥洛夫、阿根廷裔数学家蔡廷，分别从完全不同的理论视角出发，共同建立了算法信息论（Algorithmic Information Theory, AIT）。

就在所罗门诺夫的理论发表后不久后的1965年，柯尔莫哥洛夫发表了自己的核心成果——柯尔莫哥洛夫复杂性，这是整个算法信息论的核心奠基性概念，它试图回答一个听起来很简单，但在香农的信息论框架内始终没有标准答案的问题：“一个信息串的本质复杂度，到底由什么决定？”

柯尔莫哥洛夫从算法角度重新构建了信息论，核心思想是：对一个信息串（如一段文本、一张图片、一个数列）的本质复杂度度量，不取决于它的表面长度或统计特征，而取决于在某个通用图灵机上，生成这个字符串并且停机的最短程序的长度。这一长度是对字符串本身的内在模式复杂度的绝对量化，完全独立于编码方式、语言差异或其他外部因素。简言之，对字符串，能生成这个字符串且最短的图灵机程序的长度就是字符串最本质的复杂度。

就在柯尔莫哥洛夫发表这一理论结果的同时，所罗门诺夫和蔡廷也已经独立地得到了完全一致的理论结论。但柯尔莫哥洛夫的论文是三人中最早发表的，再加上他之前在数学界的权威性（苏联在数学界享有盛誉），这一复杂度定义最终被命名为 “柯尔莫哥洛夫复杂度”。

举个例子对比一下就会比较清楚：

对一个存在明显重复模式的字符串，比如 “abababababab”，其柯尔莫哥洛夫复杂度是较低的，因为可以用 “一个循环语句重复打印 'ab' 六次” 这样的短程序来生成；

对一个完全没有可识别模式的随机字符串，比如 “kfjeowp93mfcz0v”，其柯尔莫哥洛夫复杂度会很高，甚至接近字符串本身的长度，因为不存在任何可以生成它的短程序，只能写一个“按顺序逐个打印”的程序。

因而什么是随机？柯尔莫哥洛夫认为，随机就是找不到比直接打印字符串本身更短的描述。压缩就是能找到比直接打印字符串本身更短的描述。随机就是不可压缩的。随机字符串没有任何可被压缩的有效模式；任何试图对它进行压缩的算法，都必然会造成信息的丢失。更规范化的说法是，一个有限字符串被称为 “柯尔莫哥洛夫随机”（或算法随机），当且仅当它的最短描述长度，即它的柯尔莫哥洛夫复杂度不短于它本身的实际长度。

这一对 “随机性” 的定义，包含关于 “有用信息”或者”有价值的信息”的重要推论：完全随机的字符串，必然是高度不可压缩的，但这并不意味着它包含有价值的信息；相反，真正包含有价值信息的字符串，比如一本学术专著、一段逻辑严密的论证过程，必然存在某种可被理解和复现的逻辑结构，因此它的柯尔莫哥洛夫复杂度，必然会显著低于其本身的长度。

所以为什么大模型能够从海量文本中提取有价值的信息，例如语言的模式？就是因为真正有价值的人类语言和知识内容，都不是算法随机的；它们内在的语法规则、逻辑关联和知识结构，本身就是可被压缩的有效模式。大模型的学习就是一种压缩。

再提一下1966年蔡廷(Chaitin）的工作。他想要回答的问题是：如何命名一个整数呢？他的结论是：绝大多数整数最短的命名方式就是直接打印本身，没有更短的程序表示。其实，看到这里，不知道大家会不会不禁想到，很多重大的科学理论突破，其实都是对我们日常习以为常的事物进行深入思考和考察而来的。什么”随机性“、”压缩“、”对数进行命名”，我们怎么会去考虑这些事（这些有什么可想的呢？）。

就在柯尔莫哥洛夫、所罗门诺夫和蔡廷独立提出算法信息论的核心思想后，还有一个关键的理论问题：柯尔莫哥洛夫复杂度的定义，依赖于 “某个通用图灵机” 。难道一个字符串的柯尔莫哥洛夫复杂度，会随着所使用的通用图灵机的不同，即生成语言的不同而发生本质性的变化吗？那这个概念就失去了意义。

所幸，三人几乎在同一时间各自独立证明了一个后来被统称为 “所罗门诺夫 - 柯尔莫哥洛夫 - 蔡廷不变性定理” ，具体的定理内容如下：对任意两个不同的通用图灵机 U₁和 U₂，必然存在一个仅与 U₁和 U₂的指令集差异程度相关的常数 c；对任意一个字符串 x，用 U₁计算出的 x 的柯尔莫哥洛夫复杂度 K_U₁(x)，与用 U₂计算出的 x 的柯尔莫哥洛夫复杂度 K_U₂(x)，两者的差值绝对值必然小于等于 c—— 这一差值与字符串 x 的本身长度、内容或模式完全无关，只取决于两个通用图灵机指令集的翻译程序长度。

简言之，不同的语言表示只有常量的区别，而没有本质的区别。这一定理从逻辑上彻底解决了这一理论隐患。

这意味着，虽然柯尔莫哥洛夫复杂度的绝对数值会随着所使用的通用图灵机（或编程语言）的不同而产生小幅偏差，但这些偏差都被严格限制在一个固定的常数范围内。这个常数本是两个通用图灵机之间的翻译程序长度。当被度量的字符串长度足够大时，这个小幅偏差会被稀释到可以忽略的程度。

正是这一定理的存在，让算法信息论从一个依赖特定计算模型定义的局部性概念，变成了具有普适性的通用理论。同时，根据这个定理，我们可以推断：理论上，大模型可以在不同的硬件架构、不同的框架语言下，实现等价的效果。

到这里，所罗门诺夫的通用预测器和柯尔莫哥洛夫复杂度，从理论层面定义了 “最优预测” 的逻辑和度量标准。但还有一个问题：两者在理论上都是不可计算的。由于哥德尔不完备定理，即图灵机的停机不可判定（不存在任何一种算法，能在有限时间内准确判断任意一个程序是否会停机，输出结果。也就是说有的程序会陷入永远无法停机永远无法输出结果的状态。这是严格证明了的。），没有办法遍历筛选所罗曼诺夫归纳中所有的图灵机，没有办法遍历赛选所有生成字符串的程序。补全这块理论的是前苏联数学家列昂尼德・列文。

1972年，列文在一篇只有两页的简短学术论文中，证明了两个关键性的定理：
第一个定理是大名鼎鼎的 “库克 - 列文定理”，这一定理正式确立了 NP 完全性理论的核心逻辑，为后续所有启发式搜索算法提供了理论边界；
第二个定理，是在算法信息论的框架下，提出了对所罗门诺夫归纳法进行工程化近似实现的通用搜索思路，后来被称为 “列文通用搜索”。

列文思路是，将图灵机程序的搜索空间，按照程序的长度分层枚举，将 “程序运行的时间成本” 纳入到 “程序的优先级权重” 中，这解决了 “无限空间搜索” 的问题：如果一个程序的长度很短，但需要极长的运行时间才能得到结果，那么它在搜索过程中的优先级权重，就会被适当降低；相反，那些程序长度足够短、运行时间足够少的候选程序，会被优先筛选出来作为结果。

这是将 “不可计算的全空间搜索”，转化为了 “工程上可逐步逼近的启发式搜索过程”。虽然它仍然不能严格达到所罗门诺夫定义的理论最优值，但可以在有限的算力资源和可接受的时间延迟约束下，有效逼近理论上的最优。

列文的工作是对柯尔莫哥洛夫工作的推广：如果说一个数不是纯随机的，那一定能找到这个生成程序。也就是说如果逆函数存在，总可以通过列文的通用搜索过程找到，如果一个信息是可压缩的，总能找到生成这个信息的程序。

对柯尔莫哥洛夫复杂度改进的另一项重要工作是物理学家查尔斯・H・本内特在1988年提出的 “逻辑深度”。

根据定义，柯尔莫哥洛夫复杂度只度量了一个数据对象的可压缩性程度，没有区分 “有价值的复杂模式” 与 “无价值的随机噪声”。如果一个数据对象本身是完全随机的，那么它的柯尔莫哥洛夫复杂度会很高，但它本身没有包含任何有价值的模式。换句话说，柯尔莫哥洛夫复杂度的高低，与信息的价值没有必然关联。

本内特将逻辑深度 LD (S) 定义为：生成一个字符串 S 的最短程序，在通用图灵机上的实际运行执行步骤数，也就是解压这个压缩后的最短程序所需要的实际计算工作量。逻辑深度，是对 “数据中包含的有用信息的多少”，以及 “从压缩数据中恢复出有用信息的计算工作量大小” 的综合度量：

对一个随机字符串来说，它的柯尔莫哥洛夫复杂度可能很高，但程序只是需要执行打印步骤，而没有什么实际运算步骤，逻辑深度极低；

相反，对一个包含有价值信息的字符串，比如一段逻辑严密的数学证明、一篇实验报告、一段符合人类逻辑的对话内容，虽然柯尔莫哥洛夫复杂度不一定高，但内部可能包含各种迭代和计算步骤，它的逻辑深度会显著高于同等长度的随机字符串。

这正是大模型的核心能力之一：在压缩海量训练数据的同时，保留足够多的逻辑深度信息。大模型的参数值和层级，就是对这些逻辑深度信息的编码；推理过程，就是对压缩的逻辑深度信息进行解压的计算过程。

逻辑深度还有助于我们理解为什么小模型无法完全复刻大模型的泛化能力：如果一个模型的参数规模足够小，或者层级少，那么就降低了它表征的逻辑深度上限，它没有足够的 “逻辑存储容量”，来压缩和保存海量数据中的所有复杂有用模式；当模型的参数规模逐渐增大时，或者增加层级，其可以捕捉和存储的逻辑深度上限也会随之提升，从而支撑更强的泛化能力。

同时，本内特还进一步放宽了柯尔莫哥洛夫复杂度的门槛——必须使用全局最短程序作为分析基准，找不到最优程序理论就难以落地——任意一条能生成 S 的合法程序就行（长短不限，不用最优），用这条程序的运行步数作为逻辑深度。


至此，我们梳理一下脉络。

所罗门诺夫归纳法证明了“next token prediction”是实现通用智能的最优技术路径，是最广义的学习。所有可计算任务，包括理解、生成、推理、分类等任何一种具体任务都可以在保持逻辑等价的前提下，转化为一个序列预测问题。

根据柯尔莫哥洛夫复杂度的理论，要实现对一个序列的最优预测，就是要找到能生成该序列的最短图灵机程序，也就是对该序列进行压缩。

在哥德尔不完备性、以及现实的算力和数据资源约束下，在理论和工程上，都不可能真正遍历所有程序完成加权计算。根据列文和本内特的工作，找到了可以在工程上逼近这个最短程序的压缩效果的办法。

以上正是支撑现今主流大模型的数学基础：Transformer架构是对“如何近似实现所罗曼诺夫通用归纳预测”的工程化实践；预训练的过程是对海量文本数据进行模式压缩，是在模型的参数空间中，搜索到对训练数据的压缩效果最接近理论上的柯尔莫哥洛夫复杂度的参数值；推理的过程是用这个压缩后的模型，计算下一个 token 出现的概率分布，一种近似的通用归纳预测；而自回归生成，将完整的序列生成过程，拆解为一连串单次的归纳预测步骤，将token的概率分布映射为一个可被人类理解的序列。

2023 年 8 月，Ilya Sutskever（诺奖得主辛顿的学生，原OpenAI首席科学家，后因办公室政治出局） 在伯克利理论计算机科学研究所的一次公开技术演讲中坦言：GPT 系列的技术选型，是严格按照所罗门诺夫归纳的理论逻辑设计的，就是解决next token prediction问题。

说到GPT，自然会有人想到BERT。最后简要对比下二者解决的问题：

GPT所要解决的next token prediction 是这样一个问题：给定n个序列：x1，x2 …… xn，然后让你预测x（n+1）是什么

BERT所要解决的Mask问题是这样一个问题：给定m个序列：x1，x2 …xq… xm，然后xq是被掩藏的，让你预测xq是什么

GPT和BERT 所解决的数学问题是不同的。也不禁让人感叹，数学问题的表达如此简洁，却又如此深刻。

好了，最后希望你喜欢这篇文章。