# 读懂分层存储：追求整体效率最优的艺术

## 1 分层存储概述

### 1.1 分层存储的发展

无论在单机时代还是分布式集群时代，存储系统都是计算机体系结构的基础。数据放哪，数据如何写入、如何读取，如何变更，是存储系统的核心任务。

1940 年代出现的 ENIAC 计算机，只有少量数据寄存器，用于存储即时运算数据。当输入程序和待处理数据时，需要通过改变计算机的物理接线、切换物理开关，计算机才能执行计算。所以 ENIAC 还不是一个现代意义上的通用计算机。

冯诺依曼在 ENIAC 建造中途了解其设计后，向 ENIAC 的主管当局提出这一问题。但由于 ENIAC 已在建，他也没有解决方案。而设计一台通用计算机已经在他的计划之中。到 1950 年代，他主导设计了 EDVAC 计算机，使用水银延迟线作为存储介质，是第一台具备独立存储系统的存储程序计算机，虽然仅能存储 512 个 36 bit 长度的字，访问速度极慢，可靠性也很差，缺乏经济性，但却是现代意义上的第一台电子计算机。存储系统的核心矛盾从EDVAC诞生至今，几乎从未改变：如何以较低的成本，提供高效的存储服务。计算机科学和工业界的回答是：分层存储。

分层存储的重大理论突破发生在 1962 年。曼彻斯特大学的 Tom Kilburn、D.B.G. Edwards、M.J. Lanigan、F.H. Sumner 四人，在论文《One-Level Storage System》(《单级存储系统》)中首次提出一个完整的系统级方案：将性能、成本、容量差异较大的两种存储介质，组合成一个整体；用高速存储介质作为低速介质的缓存，在两者之间建立自动的数据传输机制；最终对上层程序员屏蔽介质差异，让他们感觉如同在使用单一的大容量高速存储介质。

这个方案希望取得的效果是，让数据的访问速度，接近系统中性能最高的存储介质的访问速度；而数据的实际存储成本，接近系统中容量最大、成本最低的存储介质的成本。这也成为了后续存储体系结构设计的核心目标。

分层存储理论的第一个落地案例是曼彻斯特大学的 Atlas 计算机。它将存储系统明确划分为三层：第一层是磁芯存储器，这是当时能找到的最高速存储介质，容量为 16000 字，访问延迟约为 0.9 微秒；第二层是磁鼓存储器，作为磁芯存储器的直接后援存储，容量为 96000 字，访问延迟约为 6 毫秒；第三层是磁带库，作为离线归档存储，提供当时近乎无限的大容量备份存储。

之后分层存储理论和实践不断发展：

1965 年，剑桥大学的 Maurice Wilkes 首次提出了 Cache（高速缓存）的概念，在速度相对较慢的主存与 CPU 之间加入少量但速度极快的 SRAM，作为数据和指令的高速缓存，进一步缩小了 CPU 与主存之间的性能差距。

1968 年，IBM 在 System/360 Model 85 大型机上，首次商用了 Cache 技术，在 CPU 和主存之间加入了 16KB 的 SRAM 作为 Cache，成功将 CPU 的访存延迟压降到了接近 SRAM 的水平。

1970 年，Intel 推出了第一款商用 DRAM 芯片；随后在 1973 年，IBM 发布了基于 Winchester 技术的企业级 HDD 硬盘；这两类存储介质的出现，拉开了内存和归档存储之间的性能层级差。

1980 年代，业界又在 HDD 和 DRAM 之间，加入了 SSD 作为新的存储层级。分层存储架构，已经演变为 “Cache （高速缓存） - main memory （主存）  - SSD （固态硬盘） - HDD （硬盘）” 的四级结构；数据迁移策略也由早期的简单 LRU，演进为了基于访问频率、访问时间、业务优先级等多维度的混合算法策略。

到 1990 年代，整个计算机存储系统的金字塔型层级架构已经完全成熟，分为四个明确的层级：第一层是 CPU 寄存器和内置缓存，这是速度最快但容量最小、成本最高的层级；第二层是主存（DRAM），这是容量最大、成本相对平衡的层级；第三层是主存（DRAM），这是容量和成本都相对平衡的层级；第四层是持久化存储层（SSD/HDD），这是容量较大、成本较低的层级；最底层是归档存储层（磁带库 / 光盘库），这是容量最大、成本最低的离线存储层级。

经过四十多年的发展，在2000 年以后，数据存储需求开始爆发式增长，分层存储的架构思想，延伸到了分布式存储集群和云原生存储架构领域。对于用户而言，无需关注底层介质的差异，只需根据业务的实际性能、成本需求，选择对应的存储服务即可；不同的存储介质之间的数据的分发、复制、迁移、淘汰等所有操作，都由集群或云服务商系统自动完成。

### 1.2 分层存储的设计哲学

分层理论之所以能够有效，是因为在实践过程中，观察到计算机系统存在的「局部性原理」：时间局部性：如果一个数据或指令在最近时间点刚刚被访问过，那么在极短时间内，它被再次访问的概率会很高；空间局部性：如果一个数据或指令在某个存储地址被访问，那么它附近地址的数据或指令，在短期内被访问的概率也会很高。

分层存储正是基于「局部性原理」而提出，其设计哲学可以概括为：不追求用单一介质满足全部访问需求，而是依据数据热度、访问频率、时延要求，把数据拆分安置在不同性能和成本的层级，在性能、容量、成本三者之间取得整体效率最优。这一设计哲学拆解为以下六个方面：

1. 差异化匹配，将数据的属性与介质的能力对齐。数据不是均质的：少部分数据高频读写、要求极低延迟；而绝大部分数据冷态、极少访问，但必须持久保存。硬件介质速度、容量与成本的矛盾：高性能介质（内存、SSD）速度快，但单位成本高、容量有限；低速介质（HDD、磁带、对象存储）廉价大容量，但访问时延高。让昂贵高速的硬件承担热数据访问压力，让低速廉价的硬件承载冷数据，每一类数据落在最适配它的存储层级上。

2. 让数据流动起来，用生命周期管理驱动层级迁移。数据热度不是永久不变的。刚产生的数据往往很热，随时间推移逐步降温，从热→温→冷→归档。数据在各层之间自动流动，热数据上移到高速层，冷数据下沉到大容量层；当冷数据被重新访问时，可回调至高速层。数据所在的存储层级跟随数据使用的生命周期变化。

3. 整体效率最优，而非局部最优，更非全部最优。分层存储不追求 “每一份数据访问都最快”，而是追求整体业务的成本‑收益最优。少量热数据决定业务体验，须保证性能；占绝大多数容量的冷数据决定了整体存储开销，可以牺牲访问速度换取低成本。保障少量热数据的性能，而允许冷数据访问慢一点，换取整体成本下降。

4. 对上层透明，应用无感知。分层存储对外呈现为统一命名空间，上层应用不需要感知底层有内存、SSD、磁盘、磁带多个层级。迁移、下沉、回调由存储系统内部完成。分层是系统内部实现策略，不把存储层级的复杂度暴露给业务层。业务应用只看到完整数据集，不用关心数据实际落在哪一层。

5. 分层不是越多越好。层级过多会带来元数据膨胀、迁移开销、故障面扩大、运维复杂度上升。层级太少又失去成本收益。分层需要权衡，依据业务访问模型选择合理层级。

6. 容错与一致性贯穿全层级。多层介质可靠性不同，高速介质易失（内存断电丢失），磁盘、磁带用于持久化。分层存储要处理跨层的数据一致性、副本、校验、故障恢复等问题。性能可以分层，但可靠性、一致性不能分层。

分层存储，是承认数据访问行为的不均一，用差异化的硬件去匹配不均一的数据。将高性能留给少数热数据，大容量承载绝大多数冷数据，以局部的速度妥协换取全局成本最优。数据跟随其生命周期在各层之间流动。而业务应用只看见统一的存储视图，不必感知底层层级差异，也不必关心数据的可靠性和一致语义等细节。

## 2. 分层存储的算法

普遍存在一个认知误区：分层存储的算法 ≠ 冷热迁移/缓存淘汰算法。教科书或一些算法案例常讲的 LRU 及相关变种算法等，仅解决「数据什么时候上下迁移」的调度问题，一套可用的分层存储系统，需要有八大算法策略共同支撑：
1. 调度层：LRU/2Q/ARC/LIRS 冷热迁移算法（动态调度数据）
2. 放置层：贪心/概率预判放置算法（数据初始落点优化）
3. 预取层：自适应预取算法（消除访问数据延迟毛刺）
4. IO写策略层：直写/写回/重定向（保证分层数据的一致性）
5. 空间优化层：分层压缩、去重、GC碎片整理（提升容量的利用率）
6. 稳定性层：迁移限流、错峰防抖（解决生产IO抖动问题）
7. 规则兜底层：生命周期时序策略（标准化冷热数据的流转）
8. 可靠性层：分层副本、EC纠删、巡检校验（保障数据安全可靠）

下面简要举例说明。

### 2.1 调度算法

LRU (least Recently Used, 最近最少使用) 算法，最早由 Belady 于 1966 年提出，基于时间局部性原理，默认「最近访问的数据未来更易被复用」，以「最后访问时间戳」为唯一判断维度，当高速层容量不足时，下沉最久未被访问的冷数据至低速层；低速层数据被访问时，回迁至高速热层。

LRU 算法简单，但其问题也很突出：无法区分一次性扫描访问与真实热点数据，极易造成缓存污染；无频次感知，长期低频但近期偶然访问的数据会抢占高速层资源。为解决 LRU 缓存污染问题，IBM提出 SLRU 算法，将单一缓存/存储层拆分为试用区（Probation）+ 保护区（Protected）。新数据、下沉数据先进入试用区；二次及以上访问的数据则晋升至保护区（真正热数据）；溢出时优先淘汰试用区冷数据，彻底隔离一次性访问的无效数据。其缺点是两个区固定比例划分，无法自适应动态变化。

1994 年，Theodore Johnson、Dennis Shasha 发表论文，提出2Q（two queue，双队列算法）解决LRU在文件系统与大容量分层存储时，对流式访问、临时文件的误判问题。其逻辑是，维护两个独立队列：新数据临时队列、长期热点队列。新数据首次访问仅进入新数据临时队列，不占用核心热层资源；仅二次访问确认是有效热数据后，才晋升至长期热点队列；新数据临时队列溢出数据直接下沉冷层，杜绝临时数据污染。其缺点是，对高频低频交替的动态访问模式适配性一般，无自适应调节能力。

诞生于 1970 年代的 LFU (least Frequently Used, 最少使用) 算法，由谷歌、亚马逊存储团队改进为老化LFU：从「访问时间」改为「访问频次」，并增加时间衰减因子，历史高频但长期未访问的数据，频次权重自动降低，实现自然降温下沉，义匹配分层存储生命周期。

2003 年，Nimrod Megiddo、Dharmendra S. Modha 提出 ARC （adaptive replacement cache，自适应缓存替换） 算法 ，是工业界首个自适应融合 LRU + LFU 的通用算法，解决单一算法无法适配多变业务负载的问题。其逻辑是，维护四个队列：近期新数据队列、长期热点队列、近期淘汰幽灵队列、长期淘汰幽灵队列。实时监测业务访问模式，动态自适应调整时间维度/频次维度权重：短时突发负载偏向LRU，长期稳定负载偏向LFU，自动适配分层存储的冷热迁移需求。

2002 年 ，由 University of Texas 团队提出 LIRS （low reference interval set,低互引用集）算法 ，专门针对大文件流式读写、循环扫描场景优化，解决传统算法在顺序访问场景下的大规模缓存失效问题。其核心判断维度为数据互引用间隔（IRR），区分高复用数据（LIR）和低复用数据（HIR）。优先保障短间隔高频复用数据留存高速层，长间隔、一次性扫描数据快速下沉冷层，以适配大文件的分层存储场景。

2005 年，University of Wisconsin 团队在基础 Clock 算法基础上迭代优化出 Clock-Pro （时钟进阶）算法，专为超大规模千亿级对象存储设计，解决链表类算法元数据开销过高的问题。其采用环形数组 + 标记位，搭配幽灵表记录淘汰数据痕迹。通过访问标记位判断数据热度，无复杂排序逻辑，开销极低；幽灵表可预判冷数据二次访问概率，避免误下沉、误淘汰。

### 2.2 放置

放置算法是分层存储的「静态基石」，核心职责是新数据写入时的初始层级精准分配。核心分为三类策略：1. 贪心放置：优先填充高速空闲介质，最大化利用高速层性能；2. 静态规则放置：基于文件属性（大小、类型、用途）强制分层，规避大文件、冷元数据污染高速层；3. 概率预判放置：基于历史访问时序数据计算热度概率，预判未来访问频率，实现冷热数据定点落盘，从源头减少后续跨层迁移IO开销。

### 2.3 自适应预取

自适应预取是主动预判后自动调度数据：通过监测文件访问时序、读写步长、访问模式（顺序/随机），动态调整预取粒度与预取时机，提前将即将访问的冷/温数据异步回迁至高速层；同时增加预取节流机制，避免过度预取占用高速层空间、挤占热点数据资源。

### 2.4 压缩与去重

分层存储三层介质的性能、IO开销、成本属性完全不同，因此必须采用差异化的压缩与去重策略，核心逻辑如下：
1. 高速热层（NVMe）：承载高频随机读写、核心业务流量，核心诉求为低延迟、高IOPS。仅开启轻量实时哈希去重，关闭高强度压缩与全局去重，规避压缩解压、全量哈希比对带来的CPU与IO开销，优先保障业务性能。
2. 中速温层（SSD）：承载中频访问数据，核心诉求为性能与成本平衡。启用轻量快速LZ4压缩、周期增量去重，在几乎不影响读写性能的前提下，小幅提升空间利用率。
3. 低速冷层/归档层（HDD）：承载长期静态冷数据，无高频访问诉求，核心诉求为极致低成本、高空间利用率。启用高压缩率ZIP压缩、全局全量去重、快照冗余清理，彻底消除重复数据与无效冗余，最大化降低长期存储成本。

### 2.5 分层IO一致性写

分层写策略解决多层介质异步读写带来的数据不一致、数据丢失、版本冲突问题，是分层存储数据可靠的核心基础算法。针对不同业务一致性诉求，设计三类可切换的分层写算法：1. 直写策略（Write Through）：热层写入同步落盘冷层，读写强一致、无数据丢失，牺牲延迟保障可靠性；2. 写回策略（Write Back）：数据暂存热层，异步批量下沉冷层，低延迟高吞吐，依赖副本刷盘保障数据安全；3. 写重定向策略（Write Redirect）：冷层旧数据修改时，不在原位置覆盖写入，而是在热层生成新数据，旧数据标记废弃，规避跨层覆盖IO与版本冲突，是云存储主流写法。

### 2.6 分层GC与碎片整理

分层存储的数据迁移、覆盖修改、删除操作会持续产生逻辑碎片、废弃版本、残留元数据，长期运行会导致读写放大、空间浪费、访问延迟升高。分层差异化垃圾回收与碎片整理算法摒弃全局统一回收逻辑，基于三层介质的负载特性设计差异化回收策略：
1. 热层GC（NVMe）：高频、轻量、低阈值回收。以小粒度实时清理无效数据与碎片，严格控制碎片率，保障高速层始终处于最优读写状态，杜绝性能衰减。
2. 温层GC（SSD）：定时、周期、均衡回收。按照固定时间窗口执行碎片合并与无效数据清理，平衡系统开销与空间利用率，避免碎片持续累积。
3. 冷层GC（HDD/归档）：低频、批量、错峰全量整理。仅在业务低峰期执行大粒度碎片合并、废弃版本清理、元数据规整，最大化释放存储空间，同时避免GC IO抢占业务流量。

### 2.7 迁移限流防抖

传统冷热迁移算法追求迁移命中率，而不注意流量管控，在大规模集群工作中，经常持续积攒待迁移任务，在业务低峰期集中爆发，大量跨层读写IO会瞬间打满集群带宽与IOPS，抢占核心业务资源，导致高峰期性能抖动、延迟飙升，极易触发批量迁移风暴。迁移限流防抖算法用于解决「迁移引发的IO雪崩、业务抖动」的问题，是云存储落地的必备配套算法，其核心为动态限流 + 负载感知 + 错峰静默调度三重机制：
1. 负载感知判定：实时采集集群QPS、IO利用率、带宽占用、CPU负载，动态判定系统繁忙状态；
2. 动态带宽限流：根据系统空闲资源实时分配迁移带宽，杜绝固定限流的资源浪费或限流不足；
3. 忙时静默暂停：业务高峰期负载超标时，立即暂停所有冷热迁移任务，优先保障业务流量；
4. 闲时匀速错峰：低负载时段匀速分发迁移任务，打散批量迁移流量，杜绝IO风暴。

### 2.8 数据生命周期兜底

热度迁移算法是动态、实时、自适应的主动调度，但存在局限性：新数据无历史访问数据无法判热、低频震荡数据易误迁移、长期沉寂数据易滞留高速层，须依靠时序生命周期规则作为兜底策略，形成「动态智能调度+静态规则兜底」的双层闭环架构，现已成为云分层存储的通用标准策略。生命周期策略是静态、时序、确定性的兜底规则，以「最后访问时间」为核心维度，固定时序阈值强制分层流转：
1. 新数据、震荡数据由热度算法动态调度；
2. 超过固定时长未访问的数据，无视实时热度，强制逐级下沉；
3. 极致冷数据（归档级）禁止自动回迁，仅支持手动恢复，规避无效IO；
4. 动态算法与时序策略双向校验，杜绝数据层级滞留与误调度。

### 2.9 分层容错与跨层校验

传统分层存储采用「全局统一副本、统一校验」的容错范式，但不同介质的硬件可靠性、读写频率、故障影响完全不同。分层容错与跨层校验算法，基于不同层级介质的故障概率、业务影响、成本权重，建立分层容错、分层校验、全局一致的可靠性架构，解决了统一容错「高性能层不安全、低成本层高浪费」的问题。其逻辑如下：
1. 热层（NVMe）：核心业务数据、高频读写、故障影响极大，采用「三副本冗余+实时校验和+秒级巡检」，极致保障高可用与数据零丢失；
2. 温层（SSD）：中频业务数据，采用「双副本冗余+周期校验」，平衡可靠性与存储成本；
3. 冷层/归档层（HDD）：长期静态数据、无高频访问，舍弃高开销多副本，采用「EC纠删码编码+每日全量校验+坏块自动修复」，极致降低存储成本，同时保障长期数据可靠。
同时增加跨层一致性校验机制，数据跨层迁移前后强制校验哈希，杜绝迁移过程的数据损坏与丢失。

## 小结

分层存储，依赖冷热数据迁移算法的双向调度：热数据向上回迁高速层、冷数据向下下沉低成本层，以及多项分层容错、分层校验、全局一致等可靠性机制，最终实现了多层存储介质的性能与成本平衡。

分层是权衡的产物，全局最优不等于每一个局部都做到最优，允许非关键场景做出适度牺牲，换取整个系统在成本、性能、容量上的综合平衡。